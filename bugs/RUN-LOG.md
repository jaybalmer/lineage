# Auto bug-fix run log

> Append-only. One row per scripts/auto-bugfix.sh run, written by the script at each outcome.
> The morning digest reads this. Local-only (bugs/ is gitignored). Full per-run logs live at ~/Library/Logs/linestry-autobugfix/.

| When | Branch | Bug ids | Verdict | Outcome | Detail |
|------|--------|---------|---------|---------|--------|
| 2026-06-14 11:42 PDT | auto/bugfix-20260614-1142 | BUG-041 | safe | merged | PR #70 (first autonomous ship; backfilled) |
| 2026-06-15 05:00 PDT | auto/bugfix-20260615-0500 | BUG-042 | safe | merged | PR #72 (backfilled from git history) |
| 2026-06-16 05:00 PDT | auto/bugfix-20260616-0500 | BUG-047, BUG-048, BUG-049 | safe | merged | PR #80 (`dfdd6ff`, auto-merged 05:14 PT; backfilled by morning digest, script did not self-record this row) |
| 2026-06-17 05:00 PDT | auto/bugfix-20260617-0500 | BUG-064, BUG-065, BUG-073 | safe | merged | PR #91 (`f236f65`, auto-merged) + residual PR #92 (`263f11f`); backfilled by June 18 morning digest, script did not self-record this row |
| 2026-06-18 07:13 PDT | auto/bugfix-20260618-0713 | BUG-062 | safe | incomplete | Built the member-card-dots fix (verdict safe, no migration) but did NOT commit, open, or merge a PR. Uncommitted edit left on the branch (`src/components/ui/member-card-overlay.tsx`); SHIP-LOG stub still `pending`. Needs a re-run or a human to finish the ship. Backfilled by morning digest |
| 2026-06-19 05:08 PDT | auto/bugfix-20260619-0508 | (none) | n/a | empty | Branch created with zero commits ahead of main and a clean tree; the run produced no fix to ship. BUG-062 (the prior lead) had already shipped via PR #98. Recorded by the June 19 daily triage |
| 2026-06-20 05:16 PDT | auto/bugfix-20260620-0516 | (none) | n/a | empty | Branch created with zero commits ahead of main and a clean tree; the run produced no fix to ship (the prior lead BUG-062 had already shipped via PR #98; the current lead is a fresh boards/brands polish cluster). Recorded by the June 20 morning digest |
| 2026-06-21 05:04 PDT | auto/bugfix-20260621-0504 | BUG-081, BUG-084, BUG-086 | safe | merged | PR #114 (`a7c2722`, boards/brands catalog polish, auto-merged). Script did not self-record this row; backfilled by the June 22 morning digest from git history and the June 22 triage reconcile |
| 2026-06-22 05:00 PDT | auto/bugfix-20260622-0500 | BUG-080, BUG-087, BUG-088, BUG-091 | safe | merged | PR #116 (`cfce6d9`, launch UI polish batch, auto-merged; the prior lead BUG-081/084/086 had shipped the day before, so the run picked up the next auto-merge-safe brief). Verdict per `.auto-verdict.json`. Script did not self-record this row; backfilled by the June 22 morning digest |
| 2026-06-24 05:00 PDT | auto/bugfix-20260624-0500 | BUG-097, BUG-098, BUG-102 | safe | merged | PR #126 (`ecb4277`, timeline player visual polish: final-slide BrandMark white contrast dot, full-bleed centered vignette, worked_at-org name resolver; auto-merged to prod at 05:05 PDT). Verdict per `.auto-verdict.json` (safe, no migration). Script did not self-record this row; recorded by the June 24 morning digest |
| 2026-06-25 05:00 PDT | (none) | n/a | no-show | No `auto/bugfix-20260625-*` branch exists on origin or local, and no RUN-LOG row was self-recorded, so the 05:00 run did not fire (Mac likely asleep or launchd did not trigger). The pipeline-safe lead `bugs/2026-06-25-copy-format-polish-batch.md` (BUG-105/107/110/111/112) was staged by the 04:06 triage and is still unshipped, ready for the next run or a human session. Recorded by the June 25 morning digest |
| 2026-06-26 05:00 PDT | (none) | n/a | no-show / blocked | No `auto/bugfix-20260626-*` branch on origin or local, and no self-recorded row, so the 05:00 run did not fire (Mac likely asleep or launchd did not trigger). Note: the working tree was also DIRTY (the June 25 auto run built `bugs/2026-06-25-copy-format-polish-batch.md` as UNCOMMITTED edits on `main`: `brands/[slug]/page.tsx`, `brands/page.tsx`, `add-story-modal.tsx`, `toast.tsx`, plus `.auto-verdict.json`), so the pipeline would have aborted on the clean-tree gate even if launchd had fired. BLOCKED until Jay commits those edits into a PR (naming BUG-105/107/110/111/112) or discards them. Recorded by the June 26 morning digest |
| 2026-06-28 05:00 PDT | auto/bugfix-20260628-0500 | BUG-118, BUG-119 | safe | merged | PR #134 (`2eec5f6`, launch UI polish batch 3: brand riders tab label "People" -> "Riders" + hide the dead `/me/public-view` Preview link when the public timeline is off; auto-merged to prod at 05:02 PDT, after the 04:06 triage so left unreconciled until this morning digest). Client-only, no migration. Recorded by the June 28 morning digest |
| 2026-06-29 05:00 PDT | (none) | n/a | no-show | No `auto/bugfix-20260629-*` branch exists on origin or local, and no RUN-LOG row was self-recorded, so the 05:00 run did not fire (Mac likely asleep or launchd did not trigger). Additional gate: a feature branch (`feature/people-in-timeline-unclaimed-job`) is checked out locally, so even had launchd fired the pipeline's clean-`main` precondition was not met. The pipeline-safe lead `bugs/2026-06-22-launch-ui-polish-batch-2.md` (BUG-094/099/100/101) remains staged and unshipped, ready for the next run. Recorded by the June 29 morning digest |
| 2026-06-30 05:00 PDT | auto/bugfix-20260630-0500 | BUG-094, BUG-099, BUG-100, BUG-101 | safe | merged | PR #141 (`9379f47`, launch UI polish batch 2: chronological brand-page Events sort, shared MemberBadge across Riders list / avatar dropdown / membership, stack top-bar Calendula wordmark, collective "1979-present" subtitle; auto-merged to prod, AFTER the 04:06 triage so left unreconciled until this morning digest). Client-only, no migration. Script did not self-record this row; recorded by the June 30 morning digest |
| 2026-07-01 05:00 PDT | auto/bugfix-20260701-0500 | BUG-018, BUG-050, BUG-051, BUG-068 | safe | merged | PR #152 (`db84cac`, visual consistency sweep: black-on-violet contrast + /brands h1 size + unified sort-pill treatment + shared self-reported badge + feed filter order + connection-card AA blue; auto-merged to prod at 05:09 PDT, AFTER the 04:06 triage so left unreconciled until this morning digest). Client-only, no migration. Recorded by the July 1 morning digest |
| 2026-07-02 05:00 PDT | (none) | n/a | no-show / blocked | No `auto/bugfix-20260702-*` branch exists on origin or local, and no RUN-LOG row was self-recorded, so the 05:00 run did not fire (Mac likely asleep or launchd did not trigger). Additional gate: a HUMAN bug-fix session for the auto-merge lead is mid-flight (branch `fix/compare-avatars-black-BUG-024-067` checked out with an uncommitted edit to `src/app/compare/page.tsx` and a `status: pending` SHIP-LOG entry / open PR for BUG-024 + BUG-067), so the pipeline's clean-`main` precondition was not met either. `origin/main` unchanged at `db84cac` (#152). Recorded by the July 2 morning digest |
| 2026-07-03 05:04 PDT | auto/bugfix-20260703-0504 | (none) | n/a | empty | Branch created with zero commits ahead of main (`202c429`, pre-#154) and no PR opened; no `.auto-verdict.json` and no self-recorded row, so the run fired but produced nothing to ship. Likely cause: the auto-merge lead (BUG-024 + BUG-067, compare avatars) was already built by the July 2 HUMAN session and sits as commit `f47184b` on local branch `fix/compare-avatars-black-BUG-024-067`, which was never pushed (no remote branch, SHIP-LOG still `pending`); the local checkout was also on a non-main branch. Recorded by the July 3 morning digest |
| 2026-07-04 05:00 PDT | (none) | n/a | no-show | No `auto/bugfix-20260704-*` branch exists on origin or local, and no RUN-LOG row was self-recorded, so the 05:00 run did not fire (Mac likely asleep or launchd did not trigger). Additional gate: the locally checked-out branch is `feat/invited-magic-link` (the already-merged PR #155, `30b3003` squashed onto `origin/main`), not `main`, so the pipeline's clean-`main` precondition was not met either. The pipeline-safe lead `bugs/2026-07-03-launch-ui-polish-batch-4.md` (BUG-124/125/126/128) remains staged and unshipped, ready for the next run. Recorded by the July 4 morning digest |
| 2026-07-05 05:00 PDT | (none) | n/a | no-show | No `auto/bugfix-20260705-*` branch exists on origin or local, and no RUN-LOG row was self-recorded, so the 05:00 run did not fire (Mac likely asleep or launchd did not trigger). Local checkout is `main` with a clean tree, but local `main` is stale at `202c429` (trails `origin/main` `a4a2b90`, #156). The pipeline-safe lead `bugs/2026-07-03-launch-ui-polish-batch-4.md` (BUG-124/125/126/128) remains staged and unshipped, ready for the next run. Recorded by the July 5 morning digest |
| 2026-07-06 05:00 PDT | auto/bugfix-20260706-0500 | BUG-141, BUG-142 | safe | merged | PR #159 (`dd8feb7`, mobile overflow pass: `break-words` on the story title/body in story-card.tsx and the comment body in story-interactions.tsx; client-only, no migration; auto-merged to prod at 05:03 PDT, AFTER the incomplete 04:06 triage). Script did not self-record this row; recorded by the July 6 morning digest |
| 2026-07-07 05:00 PDT | (none) | n/a | no-show | No `auto/bugfix-20260707-*` branch exists on origin or local, and no RUN-LOG row was self-recorded, so the 05:00 run did not fire (Mac likely asleep or launchd did not trigger). Additional gate: the local checkout is `feature/language-cleanup-launch-framing` (the already-merged PR #160, squashed as `572ecb9`), not `main`, and local `main` trails `origin/main` (`dd8feb7` vs `572ecb9`), so even a fired run would have been blocked. The pipeline-safe lead `bugs/2026-07-03-launch-ui-polish-batch-4.md` (BUG-124 + 125 + 126 + 128) remains unbuilt. Recorded by the July 7 morning digest |
| 2026-07-09 05:00 PDT | (none) | n/a | no-show | No `auto/bugfix-20260709-*` branch exists on origin or local, and no RUN-LOG row was self-recorded, so the 05:00 run did not fire (Mac likely asleep or launchd did not trigger). Local checkout is `main` with a clean tree, but local `main` is stale at `572ecb9` (trails `origin/main` `e3193f8`, #161), so the clean-latest-`main` precondition was not met either. The pipeline-safe lead `bugs/2026-07-03-launch-ui-polish-batch-4.md` (BUG-124 + 125 + 126 + 128) remains staged and unshipped, ready for the next run. (July 8 also recorded no auto run.) Recorded by the July 9 morning digest |
| 2026-07-10 05:13 PDT | auto/bugfix-20260710-0513 | BUG-124, BUG-125, BUG-126, BUG-128 | (none recorded) | incomplete | Run fired and built the pipeline-safe lead `bugs/2026-07-03-launch-ui-polish-batch-4.md` as UNCOMMITTED edits on the branch (3 files, 14 insertions: `brands/[slug]/page.tsx`, `people/page.tsx`, `people-in-timeline.tsx`; matches the BUG-124/125/126/128 scope) but did NOT commit, open, or merge a PR: no `.auto-verdict.json`, no SHIP-LOG stub, no remote branch. Branch sits at the `origin/main` tip `20028e7` (#162), 0 commits ahead. Needs a human (or a re-run) to finish the ship; the dirty non-main checkout will also block tomorrow's 05:00 run. Recorded by the July 10 morning digest |
| 2026-07-11 05:06 PDT | auto/bugfix-20260711-0506 | BUG-125, BUG-128 (partial) | (none recorded) | incomplete | Run fired, created the branch at the `origin/main` tip (`20028e7`, #162, 0 commits ahead) and left UNCOMMITTED edits on 2 files (`src/app/people/page.tsx`, `src/components/timeline/people-in-timeline.tsx`), a partial re-application of the batch-4 lead already committed locally as `ae4aee2` on `auto/bugfix-20260710-0513` (full 4-file BUG-124/125/126/128 scope, SHIP-LOG `pending`, never pushed, no PR). The worktree copy improves on `ae4aee2` by removing the em-dash comment in `people/page.tsx`. No commit, no PR, no `.auto-verdict.json`, no remote branch. The dirty non-main checkout will block tomorrow's 05:00 run; resolution: finish the batch-4 ship from `ae4aee2` (carry the em-dash comment fix), then clean this branch. Recorded by the July 11 morning digest |
| 2026-07-12 05:00 PDT | (none) | n/a | no-show / blocked | No `auto/bugfix-20260712-*` branch exists on origin or local, and no RUN-LOG row was self-recorded, so the 05:00 run did not fire (Mac likely asleep or launchd did not trigger). It would have been blocked anyway: the local checkout is still the dirty non-main `auto/bugfix-20260711-0506`. NOTE: that worktree now holds the FULL batch-4 scope as uncommitted edits on 4 files (`brands/[slug]/page.tsx`, `people/page.tsx`, `post-card.tsx`, `people-in-timeline.tsx`; 30 insertions, 17 deletions; file mtimes July 11 ~07:33-07:37 PDT, so the remaining BUG-124 + BUG-126 work was applied after the July 11 digest ran), a superset of the 2-file partial that digest recorded. One em dash remains in a new comment in `post-card.tsx` (line ~69 of the diff) and must be replaced before commit per the standing rule. Resolution: commit the 4 files on this branch (or cherry-fold with `ae4aee2` on `auto/bugfix-20260710-0513`), open the PR naming BUG-124/125/126/128, merge; until then the dirty tree blocks every 5 AM run. Recorded by the July 12 morning digest |
| 2026-07-13 05:00 PDT | (none) | n/a | no-show / blocked | No `auto/bugfix-20260713-*` branch exists on origin or local and no RUN-LOG row was self-recorded, so the 05:00 run did not fire; it would have been blocked anyway by the same dirty non-main checkout `auto/bugfix-20260711-0506` (third consecutive blocked/no-show day). Worktree state verified today: the full batch-4 scope (BUG-124/125/126/128) is uncommitted on 4 files (30 insertions, 17 deletions) and one new em dash remains in a `post-card.tsx` comment (the "catalog.orgs" BUG-126 note), which must be replaced before commit. Resolution unchanged: finish the ship from this worktree (fix em dash, tsc, commit, push, PR naming all four BUG ids, merge), then return the checkout to `main`. Recorded by the July 13 daily triage |
| 2026-07-14 05:00 PDT | (none) | n/a | no-show / blocked | No `auto/bugfix-20260714-*` branch exists on origin or local and no RUN-LOG row was self-recorded, so the 05:00 run did not fire; it would have been blocked anyway by the same dirty non-main checkout `auto/bugfix-20260711-0506` (fourth consecutive blocked/no-show day). Worktree state verified today: the full batch-4 scope (BUG-124/125/126/128) is still uncommitted on 4 files (`brands/[slug]/page.tsx`, `people/page.tsx`, `post-card.tsx`, `people-in-timeline.tsx`), and the `post-card.tsx` em dash comment still needs replacing before commit. Resolution unchanged: finish the ship from this worktree (fix em dash, tsc, commit, push, PR naming all four BUG ids, merge), then return the checkout to `main`. Recorded by the July 14 morning digest |
| 2026-07-15 05:00 PDT | (none) | n/a | no-show / blocked | No `auto/bugfix-20260715-*` branch exists on origin or local and no RUN-LOG row was self-recorded, so the 05:00 run did not fire; it would have been blocked anyway by the same dirty non-main checkout `auto/bugfix-20260711-0506` (fifth consecutive blocked/no-show day). No July 15 morning digest ran either; this row is backfilled by the July 16 morning digest |
| 2026-07-16 05:00 PDT | (none) | n/a | no-show / blocked | No `auto/bugfix-20260716-*` branch exists on origin or local and no RUN-LOG row was self-recorded, so the 05:00 run did not fire; it would have been blocked anyway by the same dirty non-main checkout `auto/bugfix-20260711-0506` (sixth consecutive blocked/no-show day). Worktree state verified today: the full batch-4 scope (BUG-124/125/126/128) is still uncommitted on 4 files (30 insertions, 17 deletions) and the `post-card.tsx` em dash comment (the "catalog.orgs" BUG-126 note, line ~389) still needs replacing before commit. Resolution unchanged: finish the ship from this worktree, then return the checkout to `main`. Recorded by the July 16 morning digest |
| 2026-07-17 05:00 PDT | (none) | n/a | no-show / blocked | No `auto/bugfix-20260717-*` branch exists on origin or local and no RUN-LOG row was self-recorded, so the 05:00 run did not fire; it would have been blocked anyway by the same dirty non-main checkout `auto/bugfix-20260711-0506` (seventh consecutive blocked/no-show day). Worktree state verified today: the full batch-4 scope (BUG-124/125/126/128) is still uncommitted on 4 files (30 insertions, 17 deletions) and the `post-card.tsx` em dash comment still needs replacing before commit. Also noted: the empty pre-named branch `fix/launch-ui-polish-batch-4-BUG-124-125-126-128` (created July 10, zero commits) is not evidence of a ship. Resolution unchanged: finish the ship from the worktree, then return the checkout to `main`. Recorded by the July 17 daily triage |
| 2026-07-18 05:00 PDT | (none) | n/a | no-show / blocked | No `auto/bugfix-20260718-*` branch exists on origin or local and no RUN-LOG row was self-recorded, so the 05:00 run did not fire; it would have been blocked anyway by the same dirty non-main checkout `auto/bugfix-20260711-0506` (eighth consecutive blocked/no-show day, July 12 through 18). Worktree state verified today: the full batch-4 scope (BUG-124/125/126/128) is still uncommitted on 4 files (30 insertions, 17 deletions) and the `post-card.tsx` em dash comment (the BUG-126 "catalog.orgs" note) still needs replacing before commit. Resolution unchanged: finish the ship from the worktree (fix the em dash, tsc, commit, push, PR naming all four BUG ids, merge), then return the checkout to `main`. Recorded by the July 18 morning digest |
| 2026-07-19 05:00 PDT | (none) | n/a | no-show / blocked | No `auto/bugfix-20260719-*` branch exists on origin or local and no RUN-LOG row was self-recorded, so the 05:00 run did not fire; it would have been blocked anyway by the same dirty non-main checkout `auto/bugfix-20260711-0506` (ninth consecutive blocked/no-show day). State verified by the July 19 daily triage: the full batch-4 scope (BUG-124/125/126/128) still uncommitted on 4 files (30 insertions, 17 deletions) and the `post-card.tsx` em dash comment still needs replacing before commit. Backfilled by the July 20 morning digest |
| 2026-07-20 05:00 PDT | (none) | n/a | no-show / blocked | No `auto/bugfix-20260720-*` branch exists on origin or local and no RUN-LOG row was self-recorded, so the 05:00 run did not fire; it would have been blocked anyway by the same dirty non-main checkout `auto/bugfix-20260711-0506` (tenth consecutive blocked/no-show run, July 12 through 20). Worktree state re-verified by the July 20 daily triage: full batch-4 scope (BUG-124/125/126/128) uncommitted on 4 files (`brands/[slug]/page.tsx`, `people/page.tsx`, `post-card.tsx`, `people-in-timeline.tsx`; 30 insertions, 17 deletions), the `post-card.tsx` BUG-126 em dash comment still present, the empty pre-named `fix/launch-ui-polish-batch-4-BUG-124-125-126-128` branch still carries zero commits. Resolution unchanged: finish the ship from this worktree (fix the em dash, tsc, commit the 4 files, push, PR naming all four BUG ids, merge), return the checkout to `main`, flip ONE of the two pending SHIP-LOG entries to merged and mark the other superseded. Recorded by the July 20 morning digest |
| 2026-07-21 05:00 PDT | (none) | n/a | no-show | No `auto/bugfix-20260721-*` branch exists on origin or local and no RUN-LOG row was self-recorded, so the 05:00 run did not fire (Mac likely asleep or launchd did not trigger). This is the first morning the pipeline was fully UNBLOCKED after the ten-day batch-4 stall (checkout on `main`, clean tree, `main` == `origin/main` at `c3a45aa`, #163), so the no-show is a scheduler/wake issue, not a repo gate. The pipeline-safe lead `bugs/2026-07-07-tags-entry-scroll.md` (BUG-150) remains staged and unshipped, ready for the next run. Recorded by the July 21 morning digest |
| 2026-07-22 05:15 PDT | auto/bugfix-20260722-0515 | n/a | fired, zero commits at triage time | Branch created from the `c3a45aa` main tip at 05:15; zero commits, clean tree, no PR as of the 07:46 triage. The checkout switched onto this branch during the triage, so the run may still be ACTIVE (late fire after yesterday's no-show) or it aborted before committing. Staged lead was `bugs/2026-07-07-tags-entry-scroll.md` (BUG-150). Do not mark BUG-150 shipped until a PR naming it lands on main; the next reconcile will close the loop. Recorded by the July 22 daily triage |
| 2026-07-22 05:15 PDT | auto/bugfix-20260722-0515 | (none) | n/a | empty | Run fired (first fire since the batch-4 stall cleared) and created the branch at the `origin/main` tip (`c3a45aa`, #163) but produced nothing to ship: zero commits ahead, clean tree, no `.auto-verdict.json`, no remote branch, no PR. The pipeline-safe lead `bugs/2026-07-07-tags-entry-scroll.md` (BUG-150) remains staged and unshipped. NOTE: the checkout was left on this non-main branch (clean); return it to `main` so tomorrow's 05:00 run meets the clean-main precondition. Possible factor: the July 21 feature session (PR #164, unmerged, branch `feature/curated-member-profile`) left a pending SHIP-LOG stub. Recorded by the July 22 morning digest |
| 2026-07-23 05:00 PDT | (none) | n/a | no-show / blocked | No `auto/bugfix-20260723-*` branch exists on origin or local and no RUN-LOG row was self-recorded, so the 05:00 run did not fire (Mac likely asleep or launchd did not trigger). It would have been blocked anyway: the checkout is still parked on yesterday's stale `auto/bugfix-20260722-0515` branch (clean tree, zero commits ahead of `c3a45aa`), so the clean-main precondition was not met. Resolution: return the checkout to `main` and delete the stale auto branch so the next 05:00 run can fire on the staged lead `bugs/2026-07-07-tags-entry-scroll.md` (BUG-150). Note: the 04:06 daily triage also did not complete today (bug-triage.md still dated July 22). Recorded by the July 23 morning digest |
| 2026-07-24 05:00 PDT | (none) | n/a | no-show / blocked | No `auto/bugfix-20260724-*` branch exists on origin or local and no RUN-LOG row was self-recorded, so the 05:00 run did not fire; it would have been blocked anyway: the checkout is still parked on the stale `auto/bugfix-20260722-0515` branch (clean tree, zero commits ahead of `c3a45aa`), the same blocker as July 23 (third day running). Resolution unchanged: return the checkout to `main` and delete the stale auto branch so the next 05:00 run can fire on the staged lead `bugs/2026-07-07-tags-entry-scroll.md` (BUG-150). Note: bug-triage.md is still dated July 22 at digest time; NEXT-SESSION.md carries a triage hand-off note labeled July 24 (13:20 PDT) that matches current repo state, so a partial triage ran without setting the completion marker. Recorded by the July 24 morning digest |
| 2026-07-25 05:00 PDT | (none) | n/a | no-show / blocked | No `auto/bugfix-20260725-*` branch exists on origin or local and no RUN-LOG row was self-recorded, so the 05:00 run did not fire; it would have been blocked anyway: the checkout is still parked on the stale `auto/bugfix-20260722-0515` branch (clean tree, zero commits ahead of `c3a45aa`), the same blocker as July 23 and 24 (fourth day running). Resolution unchanged: return the checkout to `main` and delete the stale auto branch so the next 05:00 run can fire on the staged lead `bugs/2026-07-07-tags-entry-scroll.md` (BUG-150). Recorded by the July 25 morning digest. |
| 2026-07-26 05:00 PDT | (none) | n/a | no-show / blocked | No `auto/bugfix-20260726-*` branch exists on origin or local and no RUN-LOG row was self-recorded, so the 05:00 run did not fire; it would have been blocked anyway: the checkout is still parked on the stale `auto/bugfix-20260722-0515` branch (clean tree, zero commits ahead of `c3a45aa`), the fourth blocked/no-show day since the empty July 22 fire. No daily triage ran July 26 either. Resolution unchanged: return the checkout to `main` and delete the stale auto branch so the next 05:00 run can fire on the staged lead `bugs/2026-07-07-tags-entry-scroll.md` (BUG-150). Backfilled by the July 27 morning digest |
| 2026-07-27 05:00 PDT | (none) | n/a | no-show / blocked | No `auto/bugfix-20260727-*` branch exists on origin or local and no RUN-LOG row was self-recorded, so the 05:00 run did not fire; it would have been blocked anyway: the checkout remains parked on the stale `auto/bugfix-20260722-0515` branch (verified this morning: clean tree, zero commits ahead of `c3a45aa` #163), the fifth blocked/no-show day. Unblock takes 10 seconds with the tree clean: `git checkout main` then `git branch -D auto/bugfix-20260722-0515` (it carries nothing unique). The staged pipeline-safe lead `bugs/2026-07-07-tags-entry-scroll.md` (BUG-150) remains unshipped. The 07:16 daily triage DID complete today. Recorded by the July 27 morning digest |
| 2026-07-28 05:00 PDT | (none) | n/a | no-show / blocked | No `auto/bugfix-20260728-*` branch exists on origin or local and no RUN-LOG row was self-recorded, so the 05:00 run did not fire; it would have been blocked anyway on two counts: the checkout is parked on the open-PR branch `fix/stories-no-store-cache-BUG-122` (PR #165, BUG-122, awaiting merge), and a stale zero-byte `.git/index.lock` (left at 04:07 by the daily triage's read-only `git status`; the sandbox cannot unlink it) blocks every git write. Unblock: `rm ~/lineage/.git/index.lock`, merge PR #165, `git checkout main`, `git branch -D auto/bugfix-20260722-0515`, delete the fix branch after merge. The staged pipeline-safe lead `bugs/2026-07-07-tags-entry-scroll.md` (BUG-150) remains unshipped. Recorded by the July 28 morning digest |
| 2026-07-29 05:00 PDT | auto/bugfix-20260729-0500 | (none) | n/a | empty | Run FIRED (Jay's July 28 cleanup landed: the three stale lock files are gone and the `auto/bugfix-20260722-0515` + `fix/stories-no-store-cache-BUG-122` branches are deleted, so the repo gates were clear) and created the branch at the `origin/main` tip (`240aaf6`, #165), but produced nothing to ship: zero commits ahead, clean tree, no `.auto-verdict.json`, no remote branch, no PR. The staged pipeline-safe lead `bugs/2026-07-07-tags-entry-scroll.md` (BUG-150) remains unbuilt; this is the second empty fire on this lead (July 22 was the first), which suggests the script is aborting before the build step rather than failing on a repo gate. NOTE: the checkout was left parked on this non-main branch (clean); return it to `main` (`git checkout main` then `git branch -D auto/bugfix-20260729-0500`) so tomorrow's 05:00 run meets the clean-main precondition. The 04:06 daily triage did NOT complete today (bug-triage.md still dated July 28). Recorded by the July 29 morning digest |
| 2026-07-30 05:10 PDT | auto/bugfix-20260730-0510 | (none) | n/a | empty | Run FIRED and created the branch at the `origin/main` tip (`240aaf6`, #165), but produced nothing to ship: zero commits ahead, clean tree, no `.auto-verdict.json`, no remote branch, no PR. This is the THIRD consecutive empty fire on the staged lead `bugs/2026-07-07-tags-entry-scroll.md` (BUG-150) (July 22, 29, 30), which strongly suggests `scripts/auto-bugfix.sh` is aborting before the build step rather than failing a repo gate; the script itself needs a look, or hand-run BUG-150 (small P2, client-only). Yesterday's `auto/bugfix-20260729-0500` branch was also never deleted, and the checkout is again parked on today's non-main auto branch. Unblock for tomorrow: `rm ~/lineage/.git/index.lock` (a fresh zero-byte lock left at 07:32 by this digest's read-only `git status`; the sandbox cannot unlink it), then `git checkout main` and `git branch -D auto/bugfix-20260729-0500 auto/bugfix-20260730-0510`. The 04:06 daily triage did NOT complete today (bug-triage.md still dated July 28, second consecutive miss). Recorded by the July 30 morning digest |
| 2026-07-31 05:13 PDT | auto/bugfix-20260731-0513 | (none) | n/a | empty | Run FIRED and created the branch at the `origin/main` tip (`b78512e`, #168), but produced nothing to ship: zero commits ahead, clean tree, no `.auto-verdict.json`, no remote branch, no PR. FOURTH consecutive empty fire on the staged lead `bugs/2026-07-07-tags-entry-scroll.md` (BUG-150) (July 22, 29, 30, 31): `scripts/auto-bugfix.sh` is evidently aborting before the build step, not failing a repo gate; inspect the script, or hand-run BUG-150 in an attended session (small P2, client-only). The stale `auto/bugfix-20260729-0500` and `auto/bugfix-20260730-0510` branches were never deleted and the checkout is again parked on today's non-main auto branch. Unblock for tomorrow: `git checkout main` then `git branch -D auto/bugfix-20260729-0500 auto/bugfix-20260730-0510 auto/bugfix-20260731-0513` (none carry unique commits; the sandbox cannot delete git refs, so this is a Jay step). The 04:06 daily triage did NOT complete today (bug-triage.md still dated July 30; the July 30 completion was an afternoon run, so the 04:06 slot has now missed three straight days). Feature side overnight: PRs #166, #167, #168 all merged July 30 (three feature ships in one day). Recorded by the July 31 morning digest |
| 2026-08-01 05:00 PDT | (none) | n/a | no-show | No `auto/bugfix-20260801-*` branch exists on origin or local and no RUN-LOG row was self-recorded, so the 05:00 run did not fire (Mac likely asleep or launchd did not trigger). It would also have been blocked from the last four fires' cleanup being undone: the checkout is on `main` (clean) but the stale `auto/bugfix-20260729-0500`, `auto/bugfix-20260730-0510`, and `auto/bugfix-20260731-0513` branches still exist locally (none carry unique commits). The staged pipeline-safe lead `bugs/2026-07-07-tags-entry-scroll.md` (BUG-150) remains unshipped after four consecutive empty fires (July 22, 29, 30, 31): the standing read is that `scripts/auto-bugfix.sh` aborts before the build step (check `~/Library/Logs/linestry-autobugfix/` + the "[Auto bug-fix] stopped" emails), or hand-run BUG-150 in an attended session (small P2, client-only). Cleanup for Jay: `git -C ~/lineage branch -D auto/bugfix-20260729-0500 auto/bugfix-20260730-0510 auto/bugfix-20260731-0513`. The 04:06 daily triage also did NOT complete today (bug-triage.md still dated July 31). Recorded by the Aug 1 morning digest |
| 2026-08-02 05:09 PDT | auto/bugfix-20260802-0509 | (none) | n/a | empty | Run FIRED (breaks the Aug 1 no-show) and created the branch at the `origin/main` tip (`470b7f2`, #180), but produced nothing to ship: zero commits ahead of `main`, clean tree, no `.auto-verdict.json`, no remote branch, no PR. This is the FIFTH empty/no-op fire on the staged lead `bugs/2026-07-07-tags-entry-scroll.md` (BUG-150) since July 22 (empty July 22/29/30/31, no-show Aug 1, empty Aug 2): the standing read holds that `scripts/auto-bugfix.sh` aborts before the build step (check `~/Library/Logs/linestry-autobugfix/` + the "[Auto bug-fix] stopped" emails to OWNER), or hand-run BUG-150 in an attended session (small P2, client-only). The checkout is again parked on today's non-main auto branch (clean tree); stale `auto/bugfix-20260729-0500`, `-20260730-0510`, `-20260731-0513` also still exist locally, none carrying unique commits. Cleanup for Jay: `git -C ~/lineage checkout main` then `git -C ~/lineage branch -D auto/bugfix-20260729-0500 auto/bugfix-20260730-0510 auto/bugfix-20260731-0513 auto/bugfix-20260802-0509`. The 04:06 daily triage did NOT complete today (bug-triage.md still dated August 1). Feature side overnight: PRs #171 to #180 all merged Aug 1 (a ten-PR podcast-refinement + chore batch; all already SHIP-LOG merged, none carrying BUG ids). Recorded by the Aug 2 morning digest |
| 2026-08-03 05:11 PDT | auto/bugfix-20260803-0511 | (none) | n/a | empty | Per the 04:06 daily triage note (which read the run before this digest): the 05:00 job FIRED Aug 3 at 05:11 and was EMPTY AGAIN, creating `auto/bugfix-20260803-0511` at the main tip with zero commits ahead, no PR, no verdict file, and the checkout PARKED on it (clean tree, no locks). No RUN-LOG row was self-recorded by the run, and the branch is not visible on this mount (local-only on Jay's Mac). Recurring empty-fire pattern (empty July 22/29/30/31, no-show Aug 1, empty Aug 2, empty Aug 3): the branch is created BEFORE the headless `claude -p` call, so the abort is upstream of the build. Root-cause guidance unchanged: check `~/Library/Logs/linestry-autobugfix/` + the "[Auto bug-fix] stopped: <reason>" emails to OWNER (most likely the `claude` CLI erroring on PATH/auth under launchd), or hand-run the auto-merge-safe lead (now BUG-156, /people tab title, client-only) in an attended session. Cleanup for Jay: `git -C ~/lineage checkout main` then `git -C ~/lineage branch -D auto/bugfix-20260803-0511` (plus older stale auto branches; none carry unique commits). NOTE: three attended bug-fix sessions DID land this morning outside the pipeline (PRs #185/#186/#187, BUG-024/067 + BUG-150 + BUG-151/148), so bugs shipped today even though the autonomous run did not. Recorded by the Aug 3 morning digest |
| 2026-08-04 05:00 PDT | (not visible on this mount) | n/a | unknown / likely empty | The 04:08 daily triage ran BEFORE the 05:00 slot, so it could not observe today's fire. At 07:16 digest time there is no `auto/bugfix-20260804-*` branch on origin and none visible on this mount (auto branches are created local-only on Jay's Mac and this sandbox sees only the four stale remotes), and no RUN-LOG row was self-recorded by the run, so the outcome cannot be confirmed from here; on the six-week pattern (empty July 22/29/30/31, no-show Aug 1, empty Aug 2/3) the most likely outcome is another empty fire aborting before the build step. Root-cause guidance unchanged: check `~/Library/Logs/linestry-autobugfix/` + the "[Auto bug-fix] stopped: <reason>" emails to OWNER (most likely the `claude` CLI erroring on PATH/auth under launchd), or hand-run the auto-merge-safe lead (BUG-156, /people tab title, client-only, no migration) in an attended session. Checkout on `main`, clean tree. Bug side: nothing shipped overnight; `origin/main` tip stays `ef27230` (#187). Recorded by the Aug 4 morning digest |
| 2026-08-05 05:00 PDT | (not visible on this mount) | n/a | unknown / likely empty | The 04:xx daily triage ran and completed (bug-triage.md dated August 5), but it runs BEFORE the 05:00 slot, so it could not observe today's fire. At 07:15 digest time there is no `auto/bugfix-20260805-*` branch on origin and none visible on this mount (auto branches are created local-only on Jay's Mac; this sandbox sees only the four stale remotes `auto/bugfix-20260614-1142`, `-20260617-0500`, `-20260618-0713`, `-20260706-0500`), and no RUN-LOG row was self-recorded by the run, so the outcome cannot be confirmed from here. On the six-week pattern (empty July 22/29/30/31, no-show Aug 1, empty Aug 2/3, unknown Aug 4) the most likely outcome is another empty fire aborting before the build step. Root-cause guidance unchanged: check `~/Library/Logs/linestry-autobugfix/` + the "[Auto bug-fix] stopped: <reason>" emails to OWNER (most likely the `claude` CLI erroring on PATH/auth under launchd), or hand-run the auto-merge-safe lead (BUG-156, /people tab title, client-only, no migration) in an attended session. Checkout on `main`, clean tree. Bug + feature sides: nothing merged overnight; `origin/main` tip stays `d6f8733` (#189). Recorded by the Aug 5 morning digest |
| 2026-08-06 05:00 PDT | (not visible on this mount) | n/a | unknown / likely empty | The daily triage ran and COMPLETED (bug-triage.md dated August 6), but it runs BEFORE the 05:00 slot, so it could not observe today's fire. At 07:15 digest time there is no `auto/bugfix-20260806-*` branch on origin and none visible on this mount (auto branches are created local-only on Jay's Mac; this sandbox sees only the four stale remotes `auto/bugfix-20260614-1142`, `-20260617-0500`, `-20260618-0713`, `-20260706-0500`), and no RUN-LOG row was self-recorded by the run, so the outcome cannot be confirmed from here. On the six-week pattern (empty July 22/29/30/31, no-show Aug 1, empty Aug 2/3, unknown Aug 4/5) the most likely outcome is another empty fire aborting before the build step. Root-cause guidance unchanged: check `~/Library/Logs/linestry-autobugfix/` + the "[Auto bug-fix] stopped: <reason>" emails to OWNER (most likely the `claude` CLI erroring on PATH/auth under launchd), or hand-run the auto-merge-safe lead (BUG-156, /people tab title, client-only, no migration) in an attended session. Checkout on `main`, clean tree. Bug + feature sides: nothing merged overnight; `origin/main` tip stays `d6f8733` (#189). Recorded by the Aug 6 morning digest |
| 2026-08-07 05:00 PDT | (not visible on this mount) | n/a | unknown / likely empty | STEP 0 flag: the 04:xx daily triage did NOT complete today (bug-triage.md still dated August 6), so today's Gmail intake is unprocessed and new-report counts are unavailable; re-run "bug triage" in Cowork to catch up. The 05:00 slot runs after the triage; at 07:32 digest time there is no `auto/bugfix-20260807-*` branch on origin and none visible on this mount (auto branches are created local-only on Jay's Mac; this sandbox sees only the four stale remotes `auto/bugfix-20260614-1142`, `-20260617-0500`, `-20260618-0713`, `-20260706-0500`), and no RUN-LOG row was self-recorded, so the outcome cannot be confirmed from here. On the six-week pattern (empty July 22/29/30/31, no-show Aug 1, empty Aug 2/3, unknown Aug 4/5/6) the most likely outcome is another empty fire aborting before the build step. Root-cause guidance unchanged: check `~/Library/Logs/linestry-autobugfix/` + the "[Auto bug-fix] stopped: <reason>" emails to OWNER (most likely the `claude` CLI erroring on PATH/auth under launchd), or hand-run the auto-merge-safe lead (BUG-156, /people tab title, client-only, no migration) in an attended session. Checkout on `main`, clean tree. Bug + feature sides: nothing merged overnight; `origin/main` tip stays `d6f8733` (#189). Recorded by the Aug 7 morning digest |
| 2026-08-08 05:00 PDT | (not visible on this mount) | n/a | unknown / likely empty | The 04:xx daily triage ran and COMPLETED (bug-triage.md dated August 8), but it runs BEFORE the 05:00 slot, so it could not observe today's fire. At digest time there is no `auto/bugfix-20260808-*` branch on origin and none visible on this mount (auto branches are created local-only on Jay's Mac; this sandbox sees only the four stale remotes `auto/bugfix-20260614-1142`, `-20260617-0500`, `-20260618-0713`, `-20260706-0500`), and no RUN-LOG row was self-recorded by the run, so the outcome cannot be confirmed from here. On the multi-week pattern (empty July 22/29/30/31, no-show Aug 1, empty Aug 2/3, unknown Aug 4/5/6/7) the most likely outcome is another empty fire aborting before the build step. Root-cause guidance unchanged: check `~/Library/Logs/linestry-autobugfix/` plus the "[Auto bug-fix] stopped: <reason>" emails to OWNER (most likely the `claude` CLI erroring on PATH/auth under launchd), or hand-run the auto-merge-safe lead (BUG-156, /people tab title, client-only, no migration) in an attended session. Checkout on `main`, clean tree. Bug + feature sides: nothing merged overnight; `origin/main` tip stays `d6f8733` (#189). Recorded by the Aug 8 morning digest |
| 2026-08-09 05:00 PDT | (not visible on this mount) | n/a | unknown / likely empty | The 04:xx daily triage ran and COMPLETED (bug-triage.md dated August 9), but it runs BEFORE the 05:00 slot, so it could not observe today's fire. At digest time there is no `auto/bugfix-20260809-*` branch on origin and none visible on this mount (auto branches are created local-only on Jay's Mac; this sandbox sees only the four stale remotes `auto/bugfix-20260614-1142`, `-20260617-0500`, `-20260618-0713`, `-20260706-0500`), and no RUN-LOG row was self-recorded by the run, so the outcome cannot be confirmed from here. On the multi-week pattern (empty July 22/29/30/31, no-show Aug 1, empty Aug 2/3, unknown Aug 4/5/6/7/8) the most likely outcome is another empty fire aborting before the build step. Root-cause guidance unchanged: check `~/Library/Logs/linestry-autobugfix/` plus the "[Auto bug-fix] stopped: <reason>" emails to OWNER (most likely the `claude` CLI erroring on PATH/auth under launchd), or hand-run the auto-merge-safe lead (BUG-156, /people tab title, client-only, no migration) in an attended session. Checkout on `main`, clean tree. Bug + feature sides: nothing merged overnight; `origin/main` tip stays `d6f8733` (#189). Recorded by the Aug 9 morning digest |
| 2026-08-11 05:00 PDT | (not visible on this mount) | n/a | unknown / likely empty | The 04:xx daily triage ran and COMPLETED (bug-triage.md dated August 11), but it runs BEFORE the 05:00 slot, so it could not observe today's fire. At digest time there is no `auto/bugfix-20260811-*` branch on origin and none visible on this mount (auto branches are created local-only on Jay's Mac; this sandbox sees only the four stale remotes `auto/bugfix-20260614-1142`, `-20260617-0500`, `-20260618-0713`, `-20260706-0500`), and no RUN-LOG row was self-recorded by the run, so the outcome cannot be confirmed from here. On the multi-week pattern (empty July 22/29/30/31, no-show Aug 1, empty Aug 2/3, unknown Aug 4 to 9) the most likely outcome is another empty fire aborting before the build step. Root-cause guidance unchanged: check `~/Library/Logs/linestry-autobugfix/` plus the "[Auto bug-fix] stopped: <reason>" emails to OWNER (most likely the `claude` CLI erroring on PATH/auth under launchd), or hand-run the auto-merge-safe lead (BUG-156, /people tab title, client-only, no migration) in an attended session. Checkout on `main`, clean tree. Bug + feature sides: nothing merged overnight; `origin/main` tip stays `d6f8733` (#189). Recorded by the Aug 11 morning digest |
| 2026-08-12 05:00 PDT | (not visible on this mount) | n/a | unknown / likely empty | The 04:xx daily triage ran and COMPLETED (bug-triage.md dated August 12), but it runs BEFORE the 05:00 slot, so it could not observe today's fire. At digest time there is no `auto/bugfix-20260812-*` branch on origin and none visible on this mount (auto branches are created local-only on Jay's Mac; this sandbox sees only the four stale remotes `auto/bugfix-20260614-1142`, `-20260617-0500`, `-20260618-0713`, `-20260706-0500`), and no RUN-LOG row was self-recorded by the run, so the outcome cannot be confirmed from here. On the multi-week pattern (empty July 22/29/30/31, no-show Aug 1, empty Aug 2/3, unknown Aug 4 to 11) the most likely outcome is another empty fire aborting before the build step. Root-cause guidance unchanged: check `~/Library/Logs/linestry-autobugfix/` plus the "[Auto bug-fix] stopped: <reason>" emails to OWNER (most likely the `claude` CLI erroring on PATH/auth under launchd), or hand-run the auto-merge-safe lead (BUG-156, /people tab title, client-only, no migration) in an attended session. Checkout on `main`, clean tree. Bug + feature sides: nothing merged overnight; `origin/main` tip stays `d6f8733` (#189). Recorded by the Aug 12 morning digest |
| 2026-08-13 05:00 PDT | (not visible on this mount) | n/a | unknown / likely empty | The 04:xx daily triage ran and COMPLETED (bug-triage.md dated August 13), but it runs BEFORE the 05:00 slot, so it could not observe today's fire. At digest time there is no `auto/bugfix-20260813-*` branch on origin and none visible on this mount (auto branches are created local-only on Jay's Mac; this sandbox sees only the four stale remotes `auto/bugfix-20260614-1142`, `-20260617-0500`, `-20260618-0713`, `-20260706-0500`), and no RUN-LOG row was self-recorded by the run, so the outcome cannot be confirmed from here. On the multi-week pattern (empty July 22/29/30/31, no-show Aug 1, empty Aug 2/3, unknown Aug 4 to 12) the most likely outcome is another empty fire aborting before the build step. Root-cause guidance unchanged: check `~/Library/Logs/linestry-autobugfix/` plus the "[Auto bug-fix] stopped: <reason>" emails to OWNER (most likely the `claude` CLI erroring on PATH/auth under launchd), or hand-run the auto-merge-safe lead (BUG-156, /people tab title, client-only, no migration) in an attended session. Checkout on `main`, clean tree. Bug + feature sides: nothing merged overnight; `origin/main` tip stays `d6f8733` (#189). Recorded by the Aug 13 morning digest |
| 2026-08-14 05:00 PDT | (not visible on this mount) | n/a | unknown / likely empty | The 04:xx daily triage ran and COMPLETED (bug-triage.md dated August 14), but it runs BEFORE the 05:00 slot, so it could not observe today's fire. At digest time there is no `auto/bugfix-20260814-*` branch on origin and none visible on this mount (auto branches are created local-only on Jay's Mac; this sandbox sees only the four stale remotes `auto/bugfix-20260614-1142`, `-20260617-0500`, `-20260618-0713`, `-20260706-0500`), and no RUN-LOG row was self-recorded by the run, so the outcome cannot be confirmed from here. On the multi-week pattern (empty July 22/29/30/31, no-show Aug 1, empty Aug 2/3, unknown Aug 4 to 13) the most likely outcome is another empty fire aborting before the build step. Root-cause guidance unchanged: check `~/Library/Logs/linestry-autobugfix/` plus the "[Auto bug-fix] stopped: <reason>" emails to OWNER (most likely the `claude` CLI erroring on PATH/auth under launchd), or hand-run the auto-merge-safe lead (BUG-156, /people tab title, client-only, no migration) in an attended session. Checkout on `main`, clean tree. Bug + feature sides: nothing merged overnight; `origin/main` tip stays `d6f8733` (#189). Recorded by the Aug 14 morning digest |
| 2026-08-17 05:00 PDT | (not visible on this mount) | n/a | unknown / likely empty | The 04:xx daily triage ran and COMPLETED (bug-triage.md dated August 17), but it runs BEFORE the 05:00 slot, so it could not observe today's fire. At digest time there is no `auto/bugfix-20260817-*` branch on origin and none visible on this mount (auto branches are created local-only on Jay's Mac; this sandbox sees only the four stale remotes `auto/bugfix-20260614-1142`, `-20260617-0500`, `-20260618-0713`, `-20260706-0500`), and no RUN-LOG row was self-recorded by the run, so the outcome cannot be confirmed from here. No rows were self-recorded for August 15 or 16 either (no digest ran those days). On the multi-week pattern (empty July 22/29/30/31, no-show Aug 1, empty Aug 2/3, unknown Aug 4 to 14) the most likely outcome is another empty fire aborting before the build step. The pipeline has now shipped NOTHING since PR #159 on July 6, six weeks. Root-cause guidance unchanged: check `~/Library/Logs/linestry-autobugfix/` plus the "[Auto bug-fix] stopped: <reason>" emails to OWNER (most likely the `claude` CLI erroring on PATH/auth under launchd), or hand-run the auto-merge-safe lead (now BUG-160, Stack edit discoverability, client-only, no migration) in an attended session. Checkout on `main`, clean tree. Bug + feature sides: nothing merged overnight; `origin/main` tip stays `d6f8733` (#189). Recorded by the Aug 17 morning digest |
| 2026-08-18 05:00 PDT | (none created) | n/a | unknown / no-show | The 04:0x daily triage ran and COMPLETED (bug-triage.md dated August 18). No `auto/bugfix-20260818-*` branch exists locally or on origin at 07:15 digest time, so the 05:00 slot produced nothing and recorded no outcome. LIKELY CAUSE THIS TIME, and it is concrete: a stale zero-byte `.git/index.lock` was left at 04:10 by the triage run's read-only `git status` and is still present at digest time, so every git command in the repo fails and the pipeline could not even reach its clean-main precondition. The digest sandbox cannot unlink it (read-only `.git` mount). Fix in 10 seconds: `rm ~/lineage/.git/index.lock`. Four stale REMOTE auto branches persist (`origin/auto/bugfix-20260614-1142`, `-20260617-0500`, `-20260618-0713`, `-20260706-0500`), none unique, prunable. |
| 2026-08-19 05:00 PDT | (none created) | n/a | no-show | No `auto/bugfix-20260819-*` branch exists locally or on origin at daily-triage time, and no RUN-LOG row was self-recorded, so the 05:00 slot produced nothing. The August 18 blocker is GONE: `~/lineage/.git/index.lock` no longer exists and every git command ran clean this run, so the lock is not the cause this time. What remains is the standing precondition problem plus the long-running abort: the checkout sits on `feat/ftue-merged-dark-story` (merged PR #194, clean tree) rather than `main`, which fails the clean-main gate on its own. Fix for tomorrow: `git -C ~/lineage checkout main`. If the run then fires and is empty again, it is the six-week pattern, so check `~/Library/Logs/linestry-autobugfix/` plus the "[Auto bug-fix] stopped: <reason>" emails to OWNER (most likely the `claude` CLI erroring on PATH or auth under launchd), or hand-run the pipeline-safe lead (BUG-160, Stack edit discoverability, client-only, no migration) in an attended session. The four stale REMOTE auto branches persist, none unique, prunable. Recorded by the August 19 daily triage |
| 2026-08-20 05:03 PDT | auto/bugfix-20260820-0500 | BUG-144,BUG-145,BUG-146,BUG-147 | safe | merged | https://github.com/jaybalmer/lineage/pull/207 (Client-only styling and markup on the brand page and two claim modals; no migration, no _public view, no auth, payments, membership or backfill.) |
| 2026-08-21 09:31 PDT | auto/bugfix-20260821-0500 | (none) | n/a | aborted | claude headless run errored. Log: /Users/jaybalmer/Library/Logs/linestry-autobugfix/20260821-050035.log |
| 2026-08-22 05:03 PDT | auto/bugfix-20260822-0500 | BUG-175 | safe | merged | https://github.com/jaybalmer/lineage/pull/209 (Client-only change to one component (mention-episode-group.tsx); no migration, no _public view, no auth, payments or membership path touched; tsc clean.) |
| 2026-08-23 07:16 PDT | auto/bugfix-20260823-0502 | (none) | n/a | aborted | claude headless run errored. Log: /Users/jaybalmer/Library/Logs/linestry-autobugfix/20260823-050159.log |
| 2026-08-24 08:07 EDT | auto/bugfix-20260824-0800 | BUG-137,BUG-134 | safe | merged | https://github.com/jaybalmer/lineage/pull/210 (Display-only tier label maps consolidated into a new non-client src/lib/tiers.ts plus one BrandMark dotColor prop; no migration, no _public view, no auth, no payments, no membership write path.) |
| 2026-08-25 11:23 EDT | auto/bugfix-20260825-0812 | (none) | n/a | aborted | claude headless run errored. Log: /Users/jaybalmer/Library/Logs/linestry-autobugfix/20260825-080201.log |
| 2026-08-26 11:59 EDT | auto/bugfix-20260826-0819 | (none) | n/a | aborted | claude headless run errored. Log: /Users/jaybalmer/Library/Logs/linestry-autobugfix/20260826-080159.log |
| 2026-08-27 08:02 EDT | (none) | (none) | n/a | aborted | gh not authenticated (run: gh auth login). Log: /Users/jaybalmer/Library/Logs/linestry-autobugfix/20260827-080200.log |
| 2026-08-28 08:12 EDT | auto/bugfix-20260828-0801 | BUG-176 | safe | merged | https://github.com/jaybalmer/lineage/pull/211 (Client-only render gate plus a name fallback in one feed page file; no migration, no _public view definition, no write path, auth, payments or membership touched.) |
| 2026-08-29 10:37 EDT | auto/bugfix-20260829-0802 | (none) | n/a | aborted | claude headless run errored. Log: /Users/jaybalmer/Library/Logs/linestry-autobugfix/20260829-080201.log |
| 2026-08-30 21:22 PDT | auto/bugfix-20260830-2122 | (none) | n/a | empty / in flight | Run FIRED but far off schedule (21:22, not 05:00; the slot has been drifting: 08:0x EDT Aug 24 to 29, now late evening). Branch created at the `origin/main` tip (`f6ec7f8`, #211) with ZERO commits above main at digest time, and the digest itself started 21:23, one minute later, so this row may be recording a run still in flight rather than a true empty. No RUN-LOG row was self-recorded. Row added by the morning-digest reconcile. Also note the run health trend: six of the last ten slots aborted (Aug 21, 23, 25, 26, 27, 29), Aug 27 specifically on `gh not authenticated`. |
| 2026-08-30 21:27 PDT | auto/bugfix-20260830-2122 | BUG-177,BUG-178 | safe | empty | Session produced no committable change (branch created, nothing staged after excluding pre-existing untracked paths). |
| 2026-08-31 05:03 PDT | (none) | (none) | n/a | aborted | working tree has uncommitted changes to tracked files, leaving your work alone. Log: /Users/jaybalmer/Library/Logs/linestry-autobugfix/20260831-050341.log |
| 2026-09-01 05:44 PDT | (none) | (none) | n/a | aborted | working tree has uncommitted changes to tracked files, leaving your work alone. Log: /Users/jaybalmer/Library/Logs/linestry-autobugfix/20260901-051001.log |
| 2026-09-02 05:02 PDT | (none) | (none) | n/a | aborted | working tree has uncommitted changes to tracked files, leaving your work alone. Log: /Users/jaybalmer/Library/Logs/linestry-autobugfix/20260902-050244.log |
| 2026-09-03 06:34 PDT | (none) | (none) | n/a | aborted | gh not authenticated (run: gh auth login). Log: /Users/jaybalmer/Library/Logs/linestry-autobugfix/20260903-050650.log |
| 2026-09-04 05:14 PDT | (none) | (none) | n/a | aborted | working tree has uncommitted changes to tracked files, leaving your work alone. Log: /Users/jaybalmer/Library/Logs/linestry-autobugfix/20260904-051427.log |
