# Linestry morning digest, September 4, 2026

> **TRIAGE DID NOT COMPLETE TODAY (bug-triage.md still dated September 3, 2026).** New-report intake counts are unavailable for today. Re-run "run bug triage" in Cowork so the day's Gmail intake gets processed.
>
> **This run also had no shell access,** so the three browser views could not be regenerated by their scripts. See "Needs you today" for exactly what that means.

**Overnight: nothing shipped, the 05:00 slot aborted for the fifth straight morning on the same stranded badge diff, and the reconcile caught a feature ship (PR #212, Facebook Login + legal pages) that had been sitting unreconciled since Tuesday.**

## Auto-fix run

**Fired, aborted, no build.** RUN-LOG row `2026-09-04 05:14 PDT ... aborted, working tree has uncommitted changes to tracked files, leaving your work alone` (log `~/Library/Logs/linestry-autobugfix/20260904-051427.log`). No branch, no PR, no commits.

That is **five consecutive dead runs**: Aug 31, Sep 1, Sep 2 (all three the same dirty tree), Sep 3 (gh not authenticated), Sep 4 (dirty tree again). The cause has not moved in six days and it cannot clear itself: the finished, type-clean BUG-177 + BUG-178 badge diff is still sitting uncommitted across nine tracked files. The pipeline is behaving correctly by refusing to clobber it. Nothing unattended will run until a human commits or discards that diff.

No draft PR is waiting on you. There is nothing to review, because nothing was built.

## Triage run

**Did not complete.** `bugs/bug-triage.md` still reads `Last updated: September 3, 2026`, so the 04:06 run either did not fire or did not finish. No dated hand-off note for today exists.

Intake counts for September 4 are therefore **unknown**, not zero. Do not read this as a quiet day. The last real intake was September 3: three reports, zero defects, all routed to the feature queue.

## Feature side

**PR #212, Facebook Login + Public Legal Pages, SHIPPED** (merged Sep 2, `origin/main` at `91625fe`, no migration). It was the feature-queue LEAD, staged August 31. Verified on disk this run: `src/app/privacy/page.tsx`, `src/app/terms/page.tsx` and `src/app/data-deletion/page.tsx` all exist.

**It sat unreconciled for two days.** The shipping session wrote no `bugs/SHIP-LOG.md` entry, so the September 3 triage saw #212 only as one of three no-BUG-id PRs and correctly left it alone. This run reconstructed the SHIP-LOG entries for **#212** (feature, merged), **#213** (landing copy, chore, merged) and **#214** (track bugs/features in git, chore, merged), moved the brief to Shipped in `feature-queue.md`, and repointed the lead.

That is the standing-rule failure worth naming: the reconcile keys on SHIP-LOG plus BUG ids. A feature PR with neither is invisible to it. Nothing was lost, but it took the digest rather than the triage to catch it.

**New feature lead: PB-010 Phase 5 (Announced / Future Events)**, `features/pb010-phase5-announced-events-brief.md`, ~3 to 4 hr, first PB-010 migration since Phase 1. Drafted June 16 and not re-verified since, so re-grep before trusting its line numbers.

**Owed, could not run here:** the brief file is still in `features/` (not archived), and the three Drive authoring copies are still in `Operations/` (not moved to `Operations/Archive/`, not indexed). Both are file moves and this run had file tools only, no shell. Two minutes at the next attended session; flagged in `NEXT-FEATURE.md` so nobody re-pulls a delivered brief in the meantime.

## Feature and bug balance

**Last 7 days: 0 bug ships, 1 feature ship.** Most recent bug ship was PR #211 (BUG-176) on August 28, seven days ago. Most recent feature ship was PR #212 on September 2. Two chores (#213, #214) also landed, excluded from the count.

**Queued:** roughly 15 bug briefs (11 of them drafted June 17 to July 5 and not re-verified since) against 2 feature briefs (Phase 5, plus Phase 6 as four independent sub-phases). **Leads:** bug = BUG-179 (P1, attended-only), feature = PB-010 Phase 5.

**Nudge:** the balance is not lopsided toward features, it is lopsided toward *nothing shipping at all*. One PR in seven days on either side, and the reason is a single uncommitted diff that has killed five overnight runs. **An open P1 (BUG-179) sitting while the only ship of the week is a feature does argue the bug side takes priority today** but the honest read is that the constraint is not prioritisation, it is the blocked pipeline. Clear the badge commit first and the overnight capacity comes back on its own.

## Suggested today

**1. START HERE: commit and ship the stranded badge cluster (BUG-177 + BUG-178).**
Not a build, a `git commit`. The work is done, the diff matches the brief, and `npx tsc --noEmit` was confirmed clean with it in place on August 31. This is the single item blocking every unattended run, and it has now cost five mornings. Review the diff, commit on the branch naming both ids, PR it, merge it, then flip the existing SHIP-LOG entry from `pending` to `merged` with `migration: none`.
Brief: `bugs/2026-08-27-unverified-badge-consistency.md`. Size: ~15 to 30 minutes.

**2. BUG-179, owner profile flips to the anonymous claim view (P1).**
The only open P1, and the highest-severity item in either queue. A signed-in member's own timeline can flip into the public view and offer "Is this you? Claim this profile". Attended and human-run by rule (auth-adjacent), so the pipeline will never take it. Diagnosis-first: step 1 is the PostHog replay `S-49`, which decides which of two verified mechanisms fired, and section 4 names a third possibility that sends it back to triage instead of to a fix. Note PR #212 just added Facebook sign-in to the auth path, so re-grep before building.
Brief: `bugs/2026-09-01-owner-profile-claim-flip.md`. Size: half a day, diagnosis first.

**3. BUG-174, finish and ship the Public Stack default work.**
Fifteen days built and unmerged on `origin/claude/public-stack-default-gating-6tzvvg` (`b75c000`), now seven PRs behind main. This is a rebase, a tsc run, a GATED `profiles` backfill and a merge, not a build. It is the oldest piece of finished-but-unshipped work in the repo and it makes every member profile shareable at `/t/[slug]`.
Brief: `bugs/2026-08-20-public-stack-default-on.md`. Size: ~2 hr, mostly rebase and migration care.

The feature lead sits below all three today purely on severity: an open P1 outranks new product work. If the P1 turns out to be a triage bounce-back rather than a fix, PB-010 Phase 5 is the next pull.

## Needs you today

- **No draft PR awaiting review.** Nothing was built overnight.
- **The badge commit** (item 1 above). It is the prerequisite for the pipeline running at all.
- **PR #214 left its own job half done.** It un-ignored `bugs/` and `features/` but committed only two READMEs, so every actual tracker is now untracked-but-not-ignored, which is worse than before: invisible to `main` AND counting as noise against the pipeline's clean-tree check. Fix is `git add bugs/ features/` and a commit, after deleting the five scratch files (`.write-test`, `.write_test`, `.writetest`, `.wt`, `.wtest`) and deciding whether `.auto-verdict.json` should be gitignored.
- **The three browser views are stale and could not be regenerated.** This run had no shell, so `render-queue.py`, `render-digest.py` and `render-features.py` could not be executed. `MORNING-DIGEST.md` (this file) is current and authoritative. `feature-queue.html` is the one that actually drifted, because the feature reconcile changed the lead and added a Shipped row. Re-run all three renderers at the next attended session.
- **Jay decisions, top three:** (1) **BUG-076**, feed flooded by individual claim cards, now three reports across eleven weeks and the most-repeated open ask in either queue; the September 3 report finally named a card shape ("Geoff added X places to their timeline") and there is a working precedent to copy in `mention-episode-group.tsx`. You own the grouping key, the threshold and the copy. (2) **Signed-in return landing**, should a returning member land on the Feed instead of `/`, or just repoint the hero CTA. (3) **BUG-053**, "My Timeline" now lives in the shared global lens row, so removing it on the landing means threading a context prop or dropping a global affordance.

## Today's leads

- **Bug lead** (`bugs/NEXT-SESSION.md`): `bugs/2026-09-01-owner-profile-claim-flip.md`, BUG-179, P1, attended and diagnosis-first.
- **Feature lead** (`features/NEXT-FEATURE.md`): `features/pb010-phase5-announced-events-brief.md`, PB-010 Phase 5, repointed this run after PR #212 shipped.
