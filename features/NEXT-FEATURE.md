# Next feature session

> This file is the single entry point for a Claude Code FEATURE session (new product
> work, not bug repairs). It is the feature-side mirror of `bugs/NEXT-SESSION.md`.
> Cowork keeps it current: build-ready feature briefs are staged into `features/` as
> dated/slugged files, "Build this" points at the lead, and the rest are listed under
> "Also ready to pull". When a session ships, the daily triage / morning digest
> reconcile flips it to shipped (off `bugs/SHIP-LOG.md` + git) and repoints the lead.
>
> Kick off in Claude Code with nothing more than: **"start a feature session"**.
> The repo `CLAUDE.md` auto-loads and points here. Each brief is self-contained;
> any product/design choice is pre-decided with a recommended default in a
> "DECISIONS (review before building)" block at the top, which Jay can override.
>
> Feature briefs live in `features/` (the repo, gitignored, mirroring `bugs/`).
> Drive `Operations/` remains the authoring/archive home; the build-ready copy is
> the one in `features/`.

---

## Status: BUILD-READY LEAD

> **September 8, 2026 (evening, Cowork with Jay). SHIPPED: funnel drop-off repairs PR 1, [#246](https://github.com/jaybalmer/lineage/pull/246). THE EPISODE-1 CODE RUN IS COMPLETE. NEW LEAD: Presenting partner surfacing.** All five items in the run are on prod: #233, #234, #235, #238 and #246. `features/funnel-drop-off-repairs-brief.md` **stays in the queue for PR 2** (R3, R5, R8), which was always scheduled after the episode, so do not archive it.
>
> **The new lead was drafted this evening from the September 7 `[Linestry Idea]` request**, the first directly commercial ask the idea lane has produced, and it is timed against episode 1 and the FNRad Season 12 deal. Jay's calls, taken live: build parts **(a)** the featured group on the brands index and **(c)** the landing-page card, defer **(b)** the curated featured page; make the mechanism **generic on the existing `orgs.curation_tier = 'founding'`** rather than an FNRad special case, so the next brand partner is a dropdown in `/admin/brand/[id]`; and build T2.4, the partner-label pill, so a founding org's card reads "Founding Brand Partner" instead of the generic "Curated".
>
> **The pre-flight found one thing that would have burned the session:** FNRad is `org_type='media'`, so it does not appear in the main brands list at all today, only in a separate "Shows & Media" section. A featured group derived from `brandOrgs` would have excluded the one org the whole request is about. Brief fact F3, decision D2.
>
> **Also reconciled this evening on the bug side** (context, not feature work): BUG-188, the localStorage quota P0, opened and closed the same day via #243; BUG-187 via #244; BUG-185 via #245. Zero open P0 and zero open P1. **BUG-189 is the open thread** from the BUG-188 build (every catalog entity listed twice in the signed-in pickers, catalog fetched twice per load) and is not yet drafted.

> _Earlier the same day:_ **September 8, 2026 (morning-digest reconcile). SHIPPED: the Attribution episode-1 carve, PR #238, plus the whole events catalog import (#239 to #242). NEW LEAD: Funnel drop-off repairs, PR 1 only.** `0294b7e`, no migration, merged on Jay's go-ahead after the auth-flow hold. First-touch capture now rides in `src/lib/attribution.ts`, is captured once after PostHog init, and is stamped onto `signup_started` and `signup_succeeded`, so **episode 1 arrives attributable**. With #233, #234, #235 and now #238 all merged, four of the five items in the episode-1 run are done and only the auth-error polish is left. The full `funnel-attribution-brief.md` STAYS in the queue: the durable `acquisition` table, the `/r/[code]` spoken route, the `ftue_landed` / `ftue_completed` stamps and the `/admin/users` column did not ship, and every line of the carve is reused by that build. **Standing instruction unchanged: do not read `/r/fnrad` on air**, the carve deliberately leaves the route out and the FNRad surfaces carry plain `?ref=fnrad` params instead. Separately, the events catalog import and its three follow-ups shipped September 7 (+119 series, +599 events, +3915 podium results, one additive migration applied, two GATED data corrections approved by Jay); its brief is archived at `features/archive/2026-09-07-events-catalog-import.md` and it never sat in this queue. No Drive authoring copies existed in `Operations/` for either, so nothing moved or was repointed there.
>
> **Still owed and still not code, unchanged:** the deploy-time acceptance walks (A1 to A3 for the handoff on the real deploy including a cross-device magic link; A9 for the listener landing, which writes prod data so use a test-episode guest; the signed-in half of A12e), and the two F18 database checks **only if the episode-1 guest changes** from Ingemar Backman (`fbf34ae7-f056-4457-ad90-3be67adac0f1`, both pre-flighted PASS in PR #235).

> _Earlier:_ **September 6, 2026 (morning-digest reconcile, run evening). SHIPPED: FNRad Listener Landing, PR #235. NEW LEAD: Attribution, episode-1 carve-out.** `6b9b5ca`, no migration, self-merged as SAFE. The lead this file pointed at all afternoon is now on prod: `/fnrad` is a public listener hub, the weekly guest-story challenge renders from one constant on every surface, and the episode pages carry a listener strip, linked mention names, linked guest chips and a logged-out CTA that finally names the guest. **With #233, #234 and #235 all merged today, the CODE path for the episode-1 challenge is complete.** Brief archived to `features/archive/2026-09-06-fnrad-listener-landing.md`; the signup-handoff and duplicate-person briefs archived alongside it; feature-queue.md Shipped section carries all four records. No Drive authoring copies existed in `Operations/` for any of the three, so there was nothing to move or repoint there.
>
> **What is left before episode 1 is NOT code, and it is Jay's.** (1) The deploy-time acceptance walks: A1 to A3 for the handoff on the real deploy including a cross-device magic link, and A9 for the listener landing (the full signed-out listener walk, which writes prod data, so use a test-episode guest) plus the signed-in half of A12e. (2) The two F18 database checks, only if the episode-1 guest changes from Ingemar Backman: PR #235 pre-flighted `require_tag_approval` and the uuid shape against `fbf34ae7-f056-4457-ad90-3be67adac0f1` and both passed, and `FNRAD_CHALLENGE.guestId` ships pointed at that verified id.
>
> **The new lead is the measurement half of the run**, and it is the smallest brief left in it: ~1.5 to 2 hr, no migration. Ship it before episode 1 airs or the highest-signal traffic moment this campaign will ever have arrives unattributed. Note the standing instruction on it: do not read `/r/fnrad` on air, because the carve deliberately leaves the `/r/[code]` route out; the FNRad surfaces already ship static `?ref=fnrad` params instead.

> _Earlier the same day:_ **September 6, 2026 (Claude Code feature session, Jay live). SHIPPED: Step 0 + Signup Intent Handoff.** Step 0 (funnel-event-completion T14) shipped alone as **PR #233** (`0c79e7a`, no migration): PostHog pageviews to `history_change` mode plus `posthog.reset()` on sign out, so episode-1 traffic is measured against a correct denominator. Then the prior lead, **Signup Intent Handoff, SHIPPED as PR #234** (`f10c5d9`, no migration): T1 to T6 all landed, `src/lib/intent.ts` plus the full signup-chain plumbing and the one-shot replay; the signed-out "Add your story about {First}" CTA and the pre-tagged composer are live. Both merged in-session (Step 0 self-merged SAFE; the handoff merged on Jay's go-ahead after the auth-flow hold). SHIP-LOG carries both as `merged`. **Deploy-time acceptance still owed** (not code): the A1 to A3 walk on the real deploy incl. cross-device magic link, and the F18 guest check (episode-1 guest must have a uuid `people.id` and `require_tag_approval` not true). **Build-this repoints to `features/fnrad-listener-landing-brief.md`** (was item 2 in the pre-episode-1 order); the two funnel carves behind it keep their order. STEP 0 (T14) is now done, so ignore the "before any of it" T14 note below.

> **September 4, 2026, LATE evening (Cowork, Jay live). LEAD CHANGED AGAIN, and the queue is now sequenced against a hard date.** Jay reported that the FNRad deal has been announced and **episode 1 ships in one to two weeks**, carrying a community challenge that asks listeners to **add a story about that episode's guest**. That broke the challenge design staged this morning, which had scoped it as an anonymous "mark your spot" tag: `StackEntryCard`'s `TAGGABLE` set is `story, place, event` and explicitly excludes riders, and `POST /api/public/tag` accepts only those three moment kinds, so the anonymous loop cannot point at a guest. Adding a story is authenticated only, so **the challenge is an authenticated action and the entire signup chain is now on the campaign's critical path.** A trace of the current listener path found ten screens, two auth gates, and the guest's identity destroyed twice: the "Create your timeline" link on `/auth/signin` drops `returnTo` entirely, which is the first click a first-time listener makes, and `/auth/complete` then lands them on their own empty profile. Jay's calls: **sign up first then write** (not a held draft, not anonymous submission), and **capacity for four or more sessions** before the episode. One new brief written, the FNRad brief revised in place, an episode-1 carve added to attribution, R4 struck from the drop-off brief as absorbed. Full reasoning per item in `feature-queue.md`.

> **BLOCKING, and it is not code. Two database checks, owned by Jay, in section 0 of `features/fnrad-listener-landing-brief.md`.** Run both the moment the episode-1 guest is known. Either one silently breaks the challenge and neither is visible from the app. (1) The guest's `profiles.require_tag_approval`: tag visibility is permissive by default so listener stories appear on the guest's page instantly, but if that guest ever switched the toggle on, every submission goes invisible pending their one-by-one approval, and a podcast guest is exactly the person likely to have turned it on. (2) The guest's id must be UUID-shaped: `story_riders.rider_id` is `uuid NOT NULL` with the profiles FK deliberately dropped and no application-layer validation on the POST path, so a non-uuid guest 500s `POST /api/stories` for every listener. Roughly 29 `people.id` values are still non-uuid. Exact SQL and remediation are in the brief.

> **STEP 0, before any of it, ~10 minutes.** Carve task T14 out of `features/funnel-event-completion-brief.md` and ship it alone: PostHog pageview capture to history-change mode in `src/instrumentation-client.ts` plus `posthog.reset()` on sign out. Two lines in two files, colliding with nothing. Without it every in-site navigation is uncounted, so episode-1 traffic gets measured against a denominator wrong by an unknown multiple. Run `npx tsc --noEmit` on the string literal: `capture_pageview` is not a declared field in the posthog-js 1.378.1 exported config type.

> **ALSO BLOCKING, still.** Nothing has merged since PR #214 on September 2, and the overnight pipeline has five consecutive dead runs on a stranded uncommitted diff (the BUG-177 / BUG-178 badge work). That diff also shifts line numbers in files these briefs cite. Clear it before pulling anything.

> **September 4, 2026, evening (Cowork, Jay live). LEAD CHANGED to FNRad Listener Landing, and FIVE new briefs staged off a funnel and analytics audit.** Jay asked for a deep dive on the onboarding funnel and analytics ahead of promoting the page through the FNRad podcast. The short version of the finding: the Diagnostics Phase 1 substrate is sound and confirmed live in production, but six of the eighteen steps between "hears an FNRad callout" and "still here next week" emit nothing, and four of those six sit in the stretch a paid campaign is trying to move. There is ZERO acquisition capture anywhere in the codebase, so the sponsorship cannot be evaluated. Five briefs are staged in `features/`, all in repo brief format with file-and-line provenance, cross-verified against `main` at `39f169d` with zero failed facts. **PB-010 Phase 5 is demoted to seventh**; Jay said "your call on the value / priority" and the reasoning is written out on its entry in `feature-queue.md`. Short form: Phase 5 deepens the experience for owners who already have a public timeline and something to announce, which is a small population today, and it does nothing for a cold listener; the funnel work changes whether new arrivals become members at all, and paid distribution is switching on this month. **Do Step 0 first regardless of which lead you pull** (see below). Full report published as a Cowork artifact, "Linestry Funnel Audit".

> **STEP 0, before any of it, ~10 minutes.** Carve task T14 out of `features/funnel-event-completion-brief.md` and ship it as its own PR: switch PostHog pageview capture to history-change mode in `src/instrumentation-client.ts` and add `posthog.reset()` on sign out. Two lines in two files, touching nothing any other brief touches. Today `capture_pageview: true` with no route-change handler means `$pageview` fires on hard loads only in an App Router app, so every in-site navigation is uncounted and every conversion rate read against that denominator is overstated. This repairs the number all four other briefs get measured against. Note `capture_pageview` is not a declared field in the posthog-js 1.378.1 exported config type, so run `npx tsc --noEmit` on the string literal before calling it a one-word fix.

> **ALSO BLOCKING, and it is not code.** Nothing has merged since PR #214 on September 2, and the overnight pipeline has produced five consecutive dead runs on a stranded uncommitted diff in the working tree (the BUG-177 / BUG-178 badge work). That diff also shifts line numbers in two files these new briefs cite. Clear it before pulling anything below.

> **September 4, 2026 (morning digest reconcile). SHIPPED: Facebook Login + Public Legal Pages merged as PR #212** (on `origin/main` at `91625fe`, Sep 2, no migration). It sat unreconciled for two days: the shipping session wrote no `bugs/SHIP-LOG.md` entry, so the Sep 3 triage saw #212 only as one of three no-BUG-id PRs and left it alone. This run reconstructed the SHIP-LOG entry, flipped it to `merged`, and moved the brief from the Queue LEAD to Shipped in `feature-queue.md`. Verified on disk: `src/app/privacy/page.tsx`, `src/app/terms/page.tsx` and `src/app/data-deletion/page.tsx` all exist. Filing is done: the brief is archived at `features/archive/2026-09-02-facebook-login-legal-pages.md`, and all three Drive authoring copies (`facebook-login-legal-pages-brief-v1.md`, `-v2.md`, and the unversioned one) moved from `Operations/` to `Operations/Archive/` with an `_INDEX.md` line mapping the brief to PR #212. No row in the Drive `CLAUDE.md` Documents table pointed at it, so there was nothing to repoint. **New LEAD by queue order: PB-010 Phase 5 (Announced / Future Events).** Worth a moment before pulling it: Jay set Phase 5 to a September target back on Aug 19, and it is now September, so it is a fair pull; but the strongest nearer-term candidate is a drafting pass on **BUG-076** (feed flooded by individual claim cards), which took its third report on Sep 3 and arrived with a proposed card shape for the first time. Jay's call.

> _Earlier:_ **August 31, 2026 (Cowork, Jay live). NEW LEAD: Facebook Login + Public Legal Pages** (`features/facebook-login-legal-pages-brief.md`). ~2 to 3 hr, ONE PR, NO migration, but with an OPS GATE (Jay in the Meta App Dashboard + Supabase) that takes a migration gate's place in the Ship sequence. Jay is registering a Meta app so members can sign in with Facebook; Meta will not accept the app configuration without a Privacy Policy URL and a Terms of Service URL, and App Review flags an app with no User Data Deletion entry. Linestry has none of the three, so this is an external blocker sitting in front of a feature Jay is actively trying to turn on. Part A is three public logged-out pages (`/privacy`, `/terms`, `/data-deletion`) off a shared shell, with the full copy locked in the brief's Appendix A, linked from the home footer, the community footer, and a consent line on both auth surfaces. Part B is "Continue with Facebook" on BOTH `signInWithOAuth` sites: `/auth/signin` AND `src/components/onboarding/save-step.tsx`, the FTUE final step, which is the primary signup path and which a one-surface brief would have missed. DECISIONS D1 to D8 with shippable defaults; D8 explicitly keeps self-serve account deletion OUT and logs it as the follow-up, because the deletion page commits to a manual 5-day/30-day promise that only holds at current volume. Authored in Drive `Operations/facebook-login-legal-pages-brief-v1.md`. PB-010 Phase 5 (already provisional at a September target) moves to "Also ready to pull". **Step 0 of the brief clears two Cowork artifacts: `features/_cowork-scratch/` and a stale `.git/index.lock`.**

> **August 20, 2026 (morning digest reconcile, 07:15). SHIPPED: the lead and its runner-up both landed overnight.** Claim-First Invite Flow merged as **PR #204** (`e741342`, no migration) and the General Feedback Form as **PR #205** (`606ef24`, additive `bug_reports.kind` migration) with copy follow-up **PR #206**. Both briefs archived to `features/archive/`, Drive authoring copies moved to `Operations/Archive/`. The first two [Linestry Idea] submissions arrived through the new lane overnight, so it works end to end. The queue ahead of PB-010 Phase 5 is now empty, so Phase 5 is lead BY DEFAULT; Jay set it to a September target on Aug 19, so treat it as provisional and consider staging a nearer-term brief first (e.g. the snowboard catalog page idea from the first [Linestry Idea] submission, in "Needs a Jay decision" in `feature-queue.md`).

> **August 19, 2026 (Cowork, Jay live, evening). LEAD CHANGED: Claim-First Invite Flow.** Jay lowered PB-010 Phase 5 to a September target ("good feature for next month") and promoted `invite-flow-claim-first-brief.md` on first-experience grounds; the General Feedback Form staged second. Both shipped that same night (see the Aug 20 note above).

> **August 19, 2026 (morning-digest reconcile, run late evening). SHIPPED as PR #203** (`416e676`, no migration). Curated Member Profile Phase 3 closed out the paid differentiator: `next/og` cards for the public member card and member profiles plus the `/membership` sell-copy pass.

> **August 18, 2026 (reconcile + Cowork, condensed). FTUE merged dark story SHIPPED as PR #194** (`9c4380a`, no migration): `/intro` and `/onboarding` merged into one forced-dark seven-beat story, `/intro` redirects to `/onboarding`. Same-day brief-to-ship.

> **August 17, 2026 (Cowork, condensed). Category Page CTA staged AND SHIPPED same day as PR #190** (`9e9a7bb`, no migration), ahead of the FNRad Season 12 sponsor announcement. Brief archived; Drive copy moved.

### Do first, ~30 to 45 min: `features/ops-tree-hygiene-brief.md`

Not product work, and short enough that it should not wait behind the lead. The 05:00 unattended run has aborted on nine of the last ten mornings because git tracks exactly three files under `bugs/` and `features/` while Cowork rewrites that directory daily, so the daily ops loop keeps disarming the nightly code loop. The wrapper starts committing ops dirt as `chore(ops)` instead of blocking on it, the auto PR is narrowed so ops prose can never ride into a fix, and PR #214 gets finished. **Hard order: script change first, `git add bugs/ features/` second.** No migration, no `src/` file, SAFE.

### Build this: `features/presenting-partner-surfacing-brief.md`

~2 to 3 hr, ONE PR, **NO migration**, no new columns, no admin work. A "Presenting Partner" group pinned above every bucket and every sort on `/[community]/brands`, plus a single presenting-partner card on the landing page. Both read `curation_tier`, `partner_label`, `logo_url` and `description` off the org row, all four already editable at `/admin/brand/[id]`.

**Why it is the lead now.** The episode-1 code run finished with #246. This is the commercial delivery for the FNRad Season 12 deal ($10K / 50,000 shares, locked) and episode 1 is one to two weeks out. It is also the cheapest brief in the queue: client render only, no migration, no auth path, no payments, so the ship sequence classifies it SAFE and the session self-merges.

**Read D2 and fact F3 before writing any code.** FNRad is `org_type='media'` and is not in the main brands list today; it renders in a separate "Shows & Media" section. Derive the featured group from `allOrgs`, not `brandOrgs`, or the feature works for every org except the one it was requested for.

**Generic by decision, not by accident.** `'curated'` stays an editorial tier; `'founding'` becomes the commercial one. Adding partner two is a dropdown, not a session. No `is_presenting_sponsor` column, deliberately.

**One thing to hand back to Jay at the end:** whether FNRad's tier is left at `founding` when the session finishes. Flipping it live on the landing page is a commercial decision, not a build decision. Brief risk R8.

**Also ready to pull, in order:** `features/funnel-drop-off-repairs-brief.md` **PR 2** (R3, R5, R8; do not rebuild R1, R2, R6 from PR 1 or R4 from PR #234), then `funnel-event-completion-brief.md` minus the already-shipped T14, then `activation-retention-scoreboard-brief.md` (~5 to 7 hr, one migration, HARD pre-merge gate), then `funnel-attribution-brief.md` **in full** (~5 to 7 hr, one migration, the durable `acquisition` table and the `/r/[code]` spoken route the carve left out), then PB-010 Phase 5 and Phase 6.

### Episode-1 run: COMPLETE

All five code items shipped: #233 (PostHog denominator), #234 (signup intent handoff), #235 (FNRad listener landing), #238 (attribution carve), #246 (drop-off repairs core). What is left before episode 1 is not code and is listed under "still owed" above.

### After episode 1, in dependency order


The rest of the funnel push. The order is dependency, not preference, and a cross-brief verification pass found three real file collisions which is what set it. Full reasoning per item in `feature-queue.md`.

1. `features/funnel-drop-off-repairs-brief.md` **PR 2 only** (~3 to 3.5 hr, no migration). The second tier of repairs: abandon-and-return losing your place, the expired-invite dead end, the sign-in wall first-time contributors hit, and the password path landing returning members on the marketing page. PR 1 and R4 have already shipped in the episode-1 run above.
2. `features/funnel-event-completion-brief.md` (~5 to 7 hr, ONE PR, no migration, minus the T14 carve-out already shipped as Step 0). Seventeen new events, the three under-fed sinks fixed, five duplicated helpers collapsed into one, plus `docs/analytics-events.md` and the `.env.example` this repo has never had. **Needs a sequencing prerequisite added before it is handed to a build session:** it is the only brief in the set with zero sibling awareness and it has the largest overlap footprint.
3. `features/activation-retention-scoreboard-brief.md` (~5 to 7 hr, ONE PR, one migration, HARD pre-merge gate). An activation definition, a return event hung off the existing daily-visit award, and `/admin/funnel`.
4. `features/funnel-attribution-brief.md` **in full** (~4 to 5 hr now that the episode-1 carve has shipped, ONE PR, one migration, soft gate). Adds what the carve deliberately left out: the durable `acquisition` table, the `ref_codes` table, the `/r/[code]` spoken route, and the `/admin/users` column. **Close one caveat before building:** task T9 has `/admin/users` read the new table, and that route returns 500 on any query error, so the read must be independently try-caught or a merge-before-migrate window 500s the admin users page.

`features/pb010-phase5-announced-events-brief.md` (PB-010 Phase 5) sits behind all of these now. It is still build-ready and it is still a good feature, but it was drafted June 16 and carries three months of tree drift, so re-grep every file and line number it names before you trust them.

> ~~Was: `features/facebook-login-legal-pages-brief.md`~~ **SHIPPED September 2 as PR #212**, archived September 4 to `features/archive/2026-09-02-facebook-login-legal-pages.md`. Do not pull it.

---

> **August 3, 2026 (morning digest reconcile).** **Podcast mention import page SHIPPED** as PR #182 (`70249a4`... see SHIP-LOG; merged Aug 2, no migration) with a copy follow-up in PR #184. The prior LEAD brief `features/podcast-import-page-brief.md` moves from lead to Shipped; brief archived (`features/archive/2026-08-02-podcast-import-page.md`). No Drive authoring copy was found in `Operations/` (staged straight into `features/`), so no Drive move; note for the weekly standup. **New LEAD: Curated Member Profile, Phase 3** (OG images + `/membership` sell copy, ~1.5 to 2 hr, no migration). The full podcast workflow (Sessions A to D + the import page) is now complete; the remaining podcast item is content: run Jay's ep-21 transcript through the skill and recreate the ep-21 page end to end as the acceptance test. Bug side overnight: PRs #185/#186/#187 (BUG-024/067, BUG-150, BUG-151/148) all shipped, reconciled in `bugs/`.

### (Historical note from Aug 3, superseded above)

> **August 1, 2026 (Claude Code session, manual).** **Podcast pass COMPLETE, and a new LEAD staged.** Sessions C and D shipped plus eight follow-ups driven by reviewing real output: PR #169 (episode as a public destination + publish_at), #170 (transcript-to-mentions skill + importer), #171 (snowboarding scoping, trimmed candidates parked by activity), #172 (story-first extraction, one story with a cast), #173 (review-page generator bundled into the skill), #174 (moment grouping on both episode surfaces), #175 (story titles, migration `20260801000001_mention_story_title.sql` applied pre-merge), #176 (connections list dropped from the episode page), #177 (near-miss detection, after the first import minted five duplicate places), #178 (per-story Publish + Publish all), #179 (mentions on place/brand/board/event pages), #181 (featured-stack cards and guests made clickable, which fixed profile and show stacks too). **Ep 21 is imported: 53 draft mentions across 18 titled stories, 18 new catalog nodes, awaiting Jay's publish.** Briefs archived. **NEW LEAD: Podcast mention import page** (`features/podcast-import-page-brief.md`), ~4 to 6 hr, NO migration. Jay wanted the workflow online so he could run it from any computer; the deciding test was running the importer under Claude Code on the web, which failed on missing credentials, and exporting a prod service-role key into an ephemeral container was correctly declined. That settled the split: the AI half (transcript to seed) stays with Claude on Jay's Max plan and costs nothing, while the data half (resolve, review, import) becomes an editor-gated admin page using his normal login, so the service-role key never leaves the server. Mostly a port of proven logic out of `scripts/import-mentions.mjs`. DECISIONS D1 to D8 with shippable defaults. Explicitly NOT adding any AI dependency to the app: there is none today.

> **August 1, 2026 (morning digest reconcile).** **Podcast pass Session C SHIPPED** as **PR #169** (`52421e7`, migration `20260731000002_event_publish_at.sql` applied pre-merge, hard gate cleared) and **Session D SHIPPED** as **PR #170** (`70249a4`, no migration), both merged July 31 evening. Both were the podcast leads; their briefs are archived (`features/archive/2026-07-31-podcast-episode-destination.md`, `features/archive/2026-07-31-podcast-transcript-mentions-skill.md`); Drive authoring copies moved to `Operations/Archive/` and indexed; both SHIP-LOG entries were already `merged`. **The integrated podcast workflow (Sessions A to D) is now complete in code.** Remaining is NOT code: hand Jay's ep-21 transcript to the `podcast-mentions` skill for the real A1 pass, then delete + recreate the ep-21 page end to end as the integrated test. **Build-this repoints to Curated Member Profile Phase 3** (OG images + `/membership` sell copy, ~1.5 to 2 hr, no migration), the next ready brief by queue order.
>
> **July 31, 2026 (Cowork, Jay live).** **Podcast pass Sessions C and D staged build-ready; Jay set the podcast workflow ahead of Curated Member Profile Phase 3, C first.** Goal is to prove the integrated transcript-to-page workflow end to end, then delete Jay's hand-built ep-21 page (`/snowboarding/events/FNRad_Jay_Balmer`) and recreate it through the workflow as the real test. **Session C: episode page as a public destination + scheduled release** (`features/podcast-episode-destination-brief.md`): published mentions + auto-surfaced linked stories on the public `/t/[slug]` episode page, show-page episode discoverability, and `events.publish_at` render-time scheduled release. ~4 to 6 hr, one additive migration (`20260731000002_event_publish_at.sql`) that is a HARD PRE-MERGE GATE (the `EVENT_STACK_COLS` explicit select 404s on a missing column). **Session D: transcript-to-mentions skill** (`features/podcast-transcript-mentions-skill-brief.md`): a `.claude/skills/podcast-mentions` authoring skill + a `podcast-seeds/` JSON format + an idempotent `scripts/import-mentions.mjs` that creates unclaimed ghosts for new subjects and imports DRAFT mentions, skip-existing. ~4 to 6 hr, no migration. Both authored in Drive `Operations/` and staged in `features/`. Build C, then D. Curated Member Profile Phase 3 moves to "Also ready to pull".
>
> **July 31, 2026 (morning digest reconcile).** **Podcast Mentions Foundation (Session B) SHIPPED** as **PR #168** (`b78512e`, squash of `f2ae427` + `68c32b5`, merged July 30 evening; migration `20260731000001_mentions.sql` applied in-session). It was the lead; the acceptance list landed except A2/A8 (the authed editor add-modal pass, owed as a quick browser check). Brief archived (`features/archive/2026-07-31-podcast-mentions-foundation.md`); Drive authoring copy mirrored to `Operations/Archive/`. **Build-this repoints to Curated Member Profile Phase 3** (OG images + `/membership` sell, ~1.5 to 2 hr, no migration), the next ready brief by queue order. Podcast Session C (episode page as a destination) is now unblocked for its Cowork drafting pass. Content follow-up, not code: populate real ep-21 mentions from the YouTube transcript, guided by the 7-entity featured set Jay curated on that episode.
>
> **July 31, 2026 (Cowork, Jay live).** Two ships reconciled and a new lead staged. **Story Date Precision SHIPPED** as **PR #166** (`eda9b63`, squash `4e81108`, merged July 30; migration `20260730000001_story_date_precision.sql` applied in-session before merge, hard pre-merge gate cleared). Brief archived (`features/archive/2026-07-30-story-date-precision.md`). **Podcast pass Session A (quickfix cluster + equity offer to end of FNRad Season 12) SHIPPED** as **PR #167** (`0481461`, merged July 30, no migration); it was built straight from `features/podcast-episode-pass-notes.md` with no staged brief, so there is no brief to archive; the notes file stays as the second-wave source. **New LEAD: Podcast Mentions Foundation** (`features/podcast-mentions-foundation-brief.md`), Session B of the podcast pass: dedicated `mentions` table (model locked by Jay, Option B), editor entry surface on the episode page, compact MentionRow on episode + person timelines with timestamped YouTube links. ~4 to 6 hr, one additive migration (`20260731000001_mentions.sql`, migrate-then-merge, NOT a hard gate). Decisions locked with Jay: mentions table over story-kind; auto-surface tagged stories on public episode pages = YES (that is Session C scope, recorded for its drafting pass); Media community type stays deferred. Authored in Drive `Operations/podcast-mentions-foundation-brief.md`.
>
> **July 30, 2026 (Cowork, Jay live).** **Story Date Precision + Editor Date Fix staged build-ready and set as LEAD per Jay.** Quick fix ahead of the future "Suggest a correction" feature: members are posting stories whose real date they don't know (they land on the timeline at today's date), and editors have no way to repair a date. Two changes: (1) partial story dates via a new additive `stories.date_precision` column ('day'/'month'/'year', padded anchor in `story_date`), DateSelect partial mode (year required, month/day optional), precision-aware display ("1998" / "Mar 1998" / "15 Mar 1998"); (2) an editor "Fix date" menu item on any story card, backed by a date-only moderator branch in `PATCH /api/stories` that mirrors the DELETE moderator boundary (is_editor only) and MUST short-circuit before the junction wipe/diff logic (see brief §4, the one data-integrity risk). ~2 to 3 hr, single PR, **migration is a hard PRE-merge gate** (write path sends the column unconditionally). DECISIONS D1 to D6 with shippable defaults. Authored in Drive `Operations/story-date-precision-brief.md`. Curated Member Profile Phase 3 moves to "Also ready to pull".
>
> **July 28, 2026 (morning digest reconcile).** **Phases 1+2 SHIPPED** as **PR #164** (`6df8ab4`, merged July 27; migration `20260721000001_curated_member_profile.sql` applied in-session before merge). Member marking + the curated layer are live in prod; the SHIP-LOG entry is `merged`. **Remaining scope for this lead is Phase 3 only: card/profile OG images + the `/membership` sell copy pass, ~1.5 to 2 hr, no migration.** The brief stays in place (not archived) and the Drive authoring copy stays in `Operations/` until Phase 3 lands. Override to PB-010 Phase 5 if you would rather bank the differentiator as-is.
>
> **July 20, 2026 (Cowork, Jay live).** **Curated Member Profile staged build-ready and set as LEAD per Jay.** The paid-vs-free differentiator: a member's profile becomes a curated personal page the way partner brands got curated pages (PR #120/#122), plus membership marking everywhere a member's name appears, the public member card surfaced from the profile, and OG unfurls. Three phases in one brief (Phase 1 marking ~1.5 hr no migration, shippable alone; Phase 2 curated layer ~3 to 4 hr with one additive migration on profiles: profile_theme, profile_statement, profile_milestones; Phase 3 card/profile OG images + membership-page sell ~1.5 to 2 hr). Gate is live membership_tier (no stored curation flag; lapse hides, data preserved). Featured rail reuses the PB-010 stack via a new public `GET /api/people/[id]/stack`. DECISIONS D1 to D9 with shippable defaults. Authored in Drive `Operations/curated-member-profile-brief.md`. PB-010 Phase 5 moves to "Also ready to pull".
>
> **July 6, 2026 (morning digest reconcile).** Two feature ships closed out on the queue. **Email Deliverability Hardening SHIPPED** as **PR #158** (`107235b`, merged July 5 evening, migration `20260705000001_email_suppressions.sql` applied): plaintext part + monitored replyTo on all 8 Resend send sites, hosted RFC 8058 one-click unsubscribe with a new `email_suppressions` table and an `isEmailSuppressed()` guard on every notification send. Brief archived (`features/archive/2026-07-05-email-deliverability-code.md`); the DNS + behavioural half stays active in Drive `Operations/email-deliverability-brief.md`. Post-deploy owner check owed: one real unsubscribe click lands a row and skips the next send. **Admin User Archive SHIPPED** as **PR #157** (`e39a23d`, merged July 5, migration `20260705000001_profiles_is_archived.sql` applied; Cy 3 archived through the new UI as acceptance proof); its deferred queue reconcile was completed this run and the brief archived (`features/archive/2026-07-05-user-archive-admin.md`). **Lead stays PB-010 Phase 5.** Also ready: Claim-First Invite Flow (`features/invite-flow-claim-first-brief.md`, ~1 to 2 hr, no schema expected).
>
> **July 3, 2026 (morning digest reconcile).** **FTUE Conversion Pass SHIPPED** as **PR #153** (`202c429`, merged July 2, no migration). It was the lead; all four tasks landed (T1 editor invite-to-claim via new `POST /api/admin/invite-node` + shared `src/lib/node-invite.ts`, T2 ClaimWelcomeOverlay for folded-in claimants, T3 first-3-stories chip + milestone, T4 land-step copy + `signup_failed` telemetry). Brief moved to feature-queue Shipped and archived (`features/archive/2026-07-02-ftue-conversion-pass.md`); the SHIP-LOG `type: feature` entry was already `merged`. Drive authoring copy mirrored to `Operations/Archive/` and indexed. **Build-this repoints to PB-010 Phase 5 (Announced / Future Events)**, the next ready brief by queue order. The remaining first-wave follow-up is NOT code: use the new editor invite surfaces on the ~12 push contacts, and seed the real FNRad show + episodes in-app.
>
> **July 2, 2026 (Cowork, Jay live).** **FTUE Conversion Pass staged build-ready and set as LEAD.** Direct support for the snowboard first-wave push going out the week of July 6 (most of the ~12 contacts already exist as unclaimed nodes). Four tasks: (T1, headline) proactive editor "Invite to claim" on `/people/[id]` + `/admin/claims` reusing the PR #138 email-first path (today only a visitor can start it); (T2) one-time "your history is already here" welcome moment for admin-invite claimants; (T3) first-3-stories progress chip + celebration (the push ask is "account + 3 stories"); (T4) funnel-cliff work from June PostHog data (34 onboarding landings to 17 land-step completions to 9 aha to 2 signups): land-step copy pass + `signup_failed` telemetry, 1 hr hard cap. Expected migration-free (PR #138's migration already added the needed columns). ~4 to 6 hr, Opus 4.8, DECISIONS block D1 to D6 with shippable defaults. Authored in Drive `Operations/ftue-conversion-pass-brief.md`.

> **June 30, 2026 (Claude Code session, manual).** **FNRad show + episode authoring SHIPPED** as **PR #144** (no migration): editors now create a media show + its episodes fully in-app (no SQL). This was the lead and is now delivered, so its brief `features/fnrad-show-episode-authoring-brief.md` moves to Shipped. With it, **the entire FNRad initiative is complete and self-serve**: Featured Timelines Phases 1 to 4 (PR #139/#140/#142/#143) + authoring (#144). The only remaining FNRad item is NOT code: seed the real FNRad show + episodes via the new in-app authoring (Brands -> + New show -> + Add episode -> curate -> publish). **No forced lead now** — pick the next item from "Also ready to pull" below, or ask Jay.
>
> **June 30, 2026 (Claude Code session, manual).** FNRad Featured Timelines COMPLETE: all four phases shipped (Phase 1 PR #139 migration `20260629000002`, Phase 2 #140, Phase 3 #142, Phase 4 #143 migration `20260630000001`). The multi-phase brief `features/event-featured-timelines-build-brief.md` is fully delivered.
>
> **June 30, 2026 (morning digest reconcile).** FNRad Featured Timelines Phase 1 (Foundation) SHIPPED as PR #139 (`ffe19d7`, migration `20260629000002` applied before merge): data-layer + types only, no UI. (Superseded by the note above now that Phases 2 to 4 have also landed.)
>
> **June 29, 2026 (morning digest reconcile).** **People In Your Timeline + Unclaimed Rider Job SHIPPED** as **PR #137** (`1a93625`, no migration): it was the prior lead, in progress during the June 29 daily triage (then on `feature/people-in-timeline-unclaimed-job`, one commit ahead of `origin/main`, not yet merged) and landed afterward, so this digest catches it. Its brief moved to feature-queue Shipped and archived (`features/archive/2026-06-28-people-in-timeline-and-unclaimed.md`); the SHIP-LOG `type: feature` entry was already `merged`. No feature session in progress (HEAD currently on the now-merged feature branch; `origin/main` at `1a93625`).
>
> **June 29, 2026 (Cowork, manual).** Event Featured Timelines (FNRad) staged build-ready and set as LEAD per Jay. Its build brief was authored in Drive `Operations/` June 25 with decisions locked, but the staging step into `features/` had never run, so the queue still showed it as not-build-ready. Copied to `features/event-featured-timelines-build-brief.md`, promoted in `feature-queue.md`, and pointed at here. PB-010 Phase 5 moves to "Also ready to pull".

~~**Lead: Curated Member Profile, Phase 3** at `features/curated-member-profile-brief.md`~~ **SHIPPED August 19 as PR [#203](https://github.com/jaybalmer/lineage/pull/203)** (`416e676`, no migration); brief archived at `features/archive/2026-08-19-curated-member-profile-phase3.md`. Historical line, superseded by the "Build this" block above. (Struck by the August 24 morning-digest reconcile.)

### Also ready to pull (ordered by readiness + value)
- `features/pb010-phase5-announced-events-brief.md` **PB-010 Phase 5 (Announced / Future Events).** Was briefly lead on the September 4 morning reconcile, **demoted to seventh the same evening** when the funnel push was staged; the reasoning is on its entry in `feature-queue.md`. Still build-ready, still a good feature, but drafted June 16 with three months of tree drift under it, so re-grep every file and line it names before trusting them. Pull it ahead of the funnel push only if an FNRad Season 12 calendar, tour or contest date becomes the thing being promoted, because announced events would then be the campaign payload rather than a nice-to-have.
- ~~`features/facebook-login-legal-pages-brief.md` -- **Facebook Login + Public Legal Pages**~~ **SHIPPED September 2 as PR #212** (no migration). Archived at `features/archive/2026-09-02-facebook-login-legal-pages.md`; do not pull. Drop this line at the next triage.
- ~~`features/invite-flow-claim-first-brief.md` -- **Claim-First Invite Flow**~~ **SHIPPED August 19 as PR [#204](https://github.com/jaybalmer/lineage/pull/204)** (`e741342`, no migration). Brief archived at `features/archive/2026-08-19-invite-flow-claim-first.md`. Left here struck through by the August 24 morning-digest reconcile so nobody pulls it; drop the line at the next triage.

### Larger / multi-session (build one sub-phase at a time)
- `features/pb010-phase6-distribution-brief.md` -- **PB-010 Phase 6 (Distribution)**. FOUR independent sub-phases, any order, each its own session + PR: **6a Embed + oEmbed** (~3 hr, lowest ops cost / highest distribution value, recommend first), **6b Custom subdomain** `{slug}.linestry.com` (~2 hr code + ops; OPS-GATED on a Vercel wildcard domain + DNS, needs a Jay/Vercel step), **6c Theming** (~3 to 4 hr, one migration), **6d Owner analytics** (~3 to 5 hr, leans on the already-wired PostHog). None blocks another; none depends on Phase 5. When pulling this, name the sub-phase (e.g. "6a").

### Feature ideas without a drafted brief yet (need a Jay decision or a Cowork drafting pass first)
- **BUG-025** (feature gap): an admin surface to manage unclaimed members (people view by `node_status`). Not a regression. Needs a Cowork drafting pass.
- **BUG-093** (feature gap): an editor/admin "Edit" affordance on an individual board page, reusing the `updateCatalogEntity` -> `POST /api/admin` path. Surface: `(community)/[community]/boards/[id]/page.tsx`. Small; needs a brief.
- **BUG-075** (needs product decision): broaden the event role beyond "Competed at" (participated / event staff / spectator). Decide the role set before scoping.
- **BUG-076** (needs design decision): aggregate same-event person-tags onto one feed card instead of one card per tag.
- **BUG-057** (parked): leaderboard / shares-distribution view. Revisit when the equity surfaces mature.

### Deferred / parked
- **PB-011 Phase 3 remaining** (`Operations/pb011-phase3-chrome-completion-brief.md`): Phase 3A (the `/me/timeline` + `/me/connections` global routes) shipped via PR #39. The heavier global-surface work (search + Cmd-K + FAB action sheet) was proposed for deferral; pull only if Jay reactivates it.
- **Board catalog seed merge** (`Operations/board-seed-merge-brief.md`): a one-off DATA operation (pure DML, no PR, no code), not a feature session. Run it as a data task, not via "start a feature session".

Recent feature ships: Podcast pass Session D: transcript-to-mentions skill (PR #170, no migration). Podcast pass Session C: episode page as a public destination + scheduled release (PR #169, migration applied pre-merge). Podcast pass Session B: mentions foundation (PR #168, migration applied in-session). Story Date Precision + editor Fix date (PR #166, migration applied pre-merge). Podcast pass Session A: episode quickfix cluster + equity offer extended to noon PT April 30, 2027, end of FNRad Season 12 (PR #167, no migration). Curated Member Profile Phases 1+2 (PR #164, migration applied). Email deliverability hardening + one-click unsubscribe (PR #158, migration applied). Admin user archive soft-hide (PR #157, migration applied, Cy 3 archived). Invited-user magic link (PR #155, closes BUG-132). FTUE Conversion Pass (PR #153, invite-to-claim + welcome + 3-stories loop + funnel telemetry). FNRad show + episode authoring (PR #144, editor self-serve, no SQL). FNRad Featured Timelines COMPLETE (Phases 1 to 4: PR #139/#140/#142/#143). Node claim by admin invite (PR #138). People In Your Timeline + Unclaimed Rider Job (PR #137). Brand Page Redesign Phase 1 + 2 (PR #120/#122, admin #123/#124). Token game-feel / earn legibility (PR #115, ~ BUG-056). Display-name cluster (PR #111/#112/#113). PB-010 Phases 1 to 4 (PR #77/#78/#81/#82/#83). Community landing Phase 2 (PR #67). Story author on_timeline toggle (PR #100). See `features/feature-queue.md` Shipped for the fuller list.

---

## Standing rules (every feature session)

- `npx tsc --noEmit` must be clean before commit.
- One PR for the session. Push the branch, open the PR.
- Run the full **Ship sequence** in the repo `CLAUDE.md` before wrapping: surface every migration as copy-paste SQL, wait for Jay to apply it, prompt and wait for the merge, then log the ship. Do not treat PR-open as the end of the session.
- Append one entry to `bugs/SHIP-LOG.md` (schema at the top of that file). Feature sessions carry no `BUG-NNN`, so use `type: feature` with `ids: none` and a `scope:` slug. This is what lets the reconcile record feature ships. Leave `status: pending`; the reconcile flips it to `merged` once the PR lands.
- No em dashes anywhere you write.
